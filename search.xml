<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>19岁随笔</title>
    <url>/2026/06/03/19/</url>
    <content><![CDATA[<p>不知不觉19啦~~ 记录一下吧</p>
<p>时光倏忽而过，恍惚刚吹完十八岁的生日蜡烛，转眼便站在了十九岁的路口。</p>
<p>回望这一年，你成长了好多好多，从前的你会因为一次考试没有考好而崩溃落泪，会因为一些小事情反复陷入精神内耗，会因为别人的看法而伤心，现在呢，慢慢的你好像强大了起来，不会因为一点点小事就埋头痛哭，遇到事情不在依靠别人，而是自己慢慢尝试解决，解决了自己很多以前感觉不可能的事情，抗压能力真的强了好多好多。但是呢，其实你还是会因为未来而焦虑，还是会因为自己的选择而苦恼，你不知道自己的选择是不是正确的，只能在心里默默承受，纠结当下的选择对错，一遍遍劝慰自己：当下就是最好的。</p>
<p>这一年在忙碌充实又焦虑迷茫交织中度过，认识了好多朋友，遇到了很好很好的室友和朋友，收获了好多。这一年经历了高考，踏入崭新的大学校园，感觉整个人都不一样了。还去了好多好多地方玩，暑假和ysh去景德镇，买了好多好多东西。和家人去了马来西亚，第一次出国玩，很新奇的体验吧。寒假去了和cjj去了南昌，南昌拌粉还有瓦罐汤真的真的好好吃，也很好玩。去比赛浙江省省赛（虽然打铁了，但也是一次很好经历），在杭州和xxy见面吃饭。五一和大学朋友们以前去了南京，南京人挤人，但是也很好玩。不知不觉去了好多地方玩啊，都说少年游历的风光自有独一份的热烈，和以后长大了去玩会有不同的感觉，等我10年后有时间还有在去一次嘿嘿</p>
<p>我有好多好多话想和你说，又好像没有什么好说的，千言万语堵在心口，落笔又不知从何说起，明年就是2开头了，好好享受最后一年1开头的时光吧！不知道以后的生活是如何的，有时候你总是幻想自己有钱了如何如何，有时候又想着只要健健康康平平淡淡的生活就好，未来的不确定性，ai的冲击力，源源不断的焦虑裹挟着你，我想等到10年后的现在，我一定要看看这封写给自己的信，我想10年后的我一定会给你解答的，你现在过的如何，是你想象的样子吗，你后来有没有为现在选择竞赛而后悔，你现在边上的人还是他吗，你和朋友们还有联系吗，你看到这个自己之前写的东西有没有哭啊，是不是很感慨啊。</p>
<p>希望以后的我看到是开心的，感动的，而不是伤心和后悔的，希望无论发生什么，你都可以坚持坚持，不要害怕，勇敢一点坚强一点，就像歌里唱的：&quot;我学着不去担心得太远 不计划太多反而能勇敢冒险~&quot;，无论你以后做出什么选择，我（19岁版）都会支持你的，因为我始终相信你，也相信我自己。</p>
<p>回头看这一路，有熬夜刷题备战的疲惫，也有取得小小成果的满心欢喜，有刚踏入大学陌生的局促，也有慢慢适应集体、被朋友温柔包容的无数瞬间。偶尔躺在床上胡思乱想，纠结前路方向，羡慕别人的步调顺遂，却忘了每个人都有自己的成长时区，不要用自己的短处和别人的长处相比较，你已经在跌跌撞撞里一步一步往前走了。不必苛责自己事事圆满，每一次经历都是收获，偶尔摆烂偷懒、短暂陷入颓废也没关系，松弛过后重新整装出发就好。十几岁最珍贵的，就是拥有试错的底气，不要因为一次失误就全盘否定自己。珍惜身边的朋友，细碎的聚餐、漫无目的的散步、随口的闲聊，都是往后很难复刻的少年时光。</p>
<p>剩下的十九岁日子，多去吹风、多看落日、多奔赴热爱，少困在无端内耗里。不需要急于敲定未来的答案，人生本就是边走边修正方向。记住此刻满腔热忱的自己吧，怀揣这份赤诚往前走。十年回望，愿你不负当下，不负十九岁满怀期许的我。愿山野长路、烟火远行仍在继续，保持热烈与赤诚，自在生长，永远拥有奔赴远方的勇气。</p>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>随笔</tag>
      </tags>
  </entry>
  <entry>
    <title>ABC457</title>
    <url>/2026/05/12/ABC457/</url>
    <content><![CDATA[<p>比赛时写到D题，比赛后补完了，记录一下第一次补完，以及第一次写题解</p>
<h1 id="A">A</h1>
<h3 id="题意">题意</h3>
<p>给你一个序列A，和下标x, 问第x个元素是多少</p>
<h3 id="思路">思路</h3>
<p>直接输出Ax，注意下标从1开始</p>
<h3 id="代码">代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> n;</span><br><span class="line">    cin&gt;&gt;n;</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">a</span><span class="params">(n<span class="number">+1</span>)</span></span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i) cin&gt;&gt;a[i];</span><br><span class="line">    <span class="type">int</span> x;</span><br><span class="line">    cin&gt;&gt;x;</span><br><span class="line">    cout&lt;&lt;a[x]&lt;&lt;<span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="B">B</h1>
<h3 id="题意-v2">题意</h3>
<p>给你N个长度不一样的序列A，和下标x，y，问Ax，y是多少</p>
<h3 id="思路-v2">思路</h3>
<p>使用vector的动态数组，注意x和y下标都从1开始</p>
<h3 id="代码-v2">代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> n;</span><br><span class="line">    cin&gt;&gt;n;</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; a[n<span class="number">+1</span>];</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> L;</span><br><span class="line">        cin&gt;&gt;L;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">0</span>;j&lt;L;++j)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> xx;</span><br><span class="line">            cin&gt;&gt;xx;</span><br><span class="line">            a[i].<span class="built_in">push_back</span>(xx);</span><br><span class="line">       &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> x,y;</span><br><span class="line">    cin&gt;&gt;x&gt;&gt;y;</span><br><span class="line">    cout&lt;&lt;a[x][y<span class="number">-1</span>]&lt;&lt;<span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="C">C</h1>
<h3 id="题意-v3">题意</h3>
<p>给你N个长度不一样的序列A，Ai的长度为Li，和一个一维数组C，然后将Ci个Ai放入B中，问Bk的值</p>
<h3 id="思路-v3">思路</h3>
<p>枚举找到k在哪一个Ai上，然后输出。注意下标k是从0开始的</p>
<h3 id="代码-v3">代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> n;</span><br><span class="line">    ll k;</span><br><span class="line">    cin&gt;&gt;n&gt;&gt;k;</span><br><span class="line">    vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">a</span>(n<span class="number">+1</span>);</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">l</span><span class="params">(n<span class="number">+1</span>)</span></span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> L;</span><br><span class="line">        cin&gt;&gt;L;</span><br><span class="line">        l[i]=L;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">0</span>;j&lt;L;++j)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> xx;</span><br><span class="line">            cin&gt;&gt;xx;</span><br><span class="line">            a[i].<span class="built_in">push_back</span>(xx);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">c</span><span class="params">(n<span class="number">+1</span>)</span></span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i) cin&gt;&gt;c[i];</span><br><span class="line">    <span class="type">int</span> idx=<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span>(;<span class="built_in">idx</span>&lt;=n;++idx)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(k&gt;(ll)l[idx]*c[idx])</span><br><span class="line">        &#123;</span><br><span class="line">            k-=l[idx]*c[idx];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;a[idx][(k<span class="number">-1</span>)%l[idx]]&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="D">D</h1>
<h3 id="题意-v4">题意</h3>
<p>给一个序列A，可以进行k次操作，每次可以给Ai加上i（下标从1开始），问最大的A中最小值是多少</p>
<h3 id="思路-v4">思路</h3>
<p>既然要最大的最小值，那肯定k次要都使用完，每次选择最小值加不就好了，于是我想到优先队列，正准备开始写，看了一下k的范围1e18？？！
好的，思路完全偏了，最大的最小值，噢！二分啊
二分答案，如果答案是mid的时候，cnt（操作的数量）&lt;=k,说明这个答案是可以的，继续寻找更大的答案
r的初始值需要设置一个极大的值，我刚开始想着r不会超过A中最小值的ki，但是可能数值太大，longlong也会爆，导致wa了</p>
<h3 id="代码-v4">代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n;</span><br><span class="line">ll k;</span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">check</span><span class="params">(<span class="type">const</span> vector&lt;ll&gt;&amp; a,ll x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ll cnt=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        ll now=a[i];</span><br><span class="line">        <span class="keyword">if</span>(now&gt;=x) <span class="keyword">continue</span>;</span><br><span class="line">        ll diff=x-now;</span><br><span class="line">        ll op=(diff+i<span class="number">-1</span>)/i;</span><br><span class="line">        cnt+=op;</span><br><span class="line">        <span class="keyword">if</span>(cnt&gt;k) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(cnt&gt;k) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cin&gt;&gt;n&gt;&gt;k;</span><br><span class="line">    <span class="function">vector&lt;ll&gt; <span class="title">a</span><span class="params">(n<span class="number">+1</span>)</span></span>;</span><br><span class="line">    ll minn=<span class="number">1e18</span>;</span><br><span class="line">    ll maxx=<span class="number">0</span>;</span><br><span class="line">    <span class="comment">//ll wz=0;</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        cin&gt;&gt;a[i];</span><br><span class="line">        minn=<span class="built_in">min</span>(a[i],minn);</span><br><span class="line">        <span class="comment">//if(a[i]==minn) wz=i;</span></span><br><span class="line">    &#125;</span><br><span class="line">    ll l=minn,r=<span class="number">2e18</span>;</span><br><span class="line">    ll ans=l;</span><br><span class="line">    <span class="keyword">while</span>(l&lt;=r)</span><br><span class="line">    &#123;</span><br><span class="line">        ll mid=(r+l)&gt;&gt;<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">check</span>(a,mid))</span><br><span class="line">        &#123;</span><br><span class="line">            l=mid<span class="number">+1</span>;</span><br><span class="line">            ans=mid;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> r=mid<span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;ans&lt;&lt;<span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="E">E</h1>
<h3 id="题意-v5">题意</h3>
<p>给你m块布，每条覆盖了Li到Ri。进行q次查询，是否可以从 M 块布中恰好选择两块布，使得Si到Ti至少有一条布覆盖，其他单元格没有被任何布覆盖</p>
<h3 id="思路-v5">思路</h3>
<p>只能覆盖Si到Ti，那么两条布一个左端点在Si，一个右端点在Ti。然后我们在左端点在Si的布里面寻找R&lt;=Ti的，右端点在Ti的布里面寻找L&gt;=Si的，因为Si到Ti里面都需要布覆盖，那我们在R中要寻找max，在L中寻找min，然后判断R&gt;=L-1
如果找到的L=Si，R=Ti，有可能是一个布
此时我们需要在寻找一个布，分为4个情况
1.l = L，r = R
2.l = L，r&lt; R
3.l &gt; L,  r = R
4.l &gt; L,  r &lt; R
对于1，我们直接使用map来计数相同的布的个数
对于2，我们可以在左端点在Si的布中直接寻找最小的r，判断r &lt; R
相同的，对于3，我们可以在右端点在Ti的布中直接寻找最大的l，判断l &gt; L
对于4，我们可以使用一个数组Right,Right[i]表示以i开始的最小r的值，然后判断Right[L+1] &lt; R</p>
<h3 id="代码-v5">代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">2e5</span><span class="number">+5</span>,inf=<span class="number">0x3f3f3f3f</span>;</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; r[N],l[N];</span><br><span class="line"><span class="type">int</span> R[N];</span><br><span class="line">map&lt;pair&lt;<span class="type">int</span>,<span class="type">int</span>&gt;,<span class="type">int</span>&gt; mp;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> n,m;</span><br><span class="line">    cin&gt;&gt;n&gt;&gt;m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> x,y;</span><br><span class="line">        cin&gt;&gt;x&gt;&gt;y;</span><br><span class="line">        mp[&#123;x,y&#125;]++;</span><br><span class="line">        r[x].<span class="built_in">push_back</span>(y);</span><br><span class="line">        l[y].<span class="built_in">push_back</span>(x);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(!r[i].<span class="built_in">empty</span>()) <span class="built_in">sort</span>(r[i].<span class="built_in">begin</span>(),r[i].<span class="built_in">end</span>());</span><br><span class="line">        <span class="keyword">if</span>(!l[i].<span class="built_in">empty</span>()) <span class="built_in">sort</span>(l[i].<span class="built_in">begin</span>(),l[i].<span class="built_in">end</span>());</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    R[n<span class="number">+1</span>]=inf;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=n;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        R[i]=R[i<span class="number">+1</span>];</span><br><span class="line">        <span class="keyword">if</span>(!r[i].<span class="built_in">empty</span>())</span><br><span class="line">        &#123;</span><br><span class="line">            R[i]=<span class="built_in">min</span>(R[i],r[i].<span class="built_in">front</span>());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="type">int</span> q;</span><br><span class="line">    cin&gt;&gt;q;</span><br><span class="line">    <span class="keyword">while</span>(q--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> x,y;</span><br><span class="line">        cin&gt;&gt;x&gt;&gt;y;</span><br><span class="line">        <span class="type">int</span> f=<span class="number">0</span>;</span><br><span class="line">        <span class="type">int</span> ll=inf,rr=<span class="number">0</span>;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">auto</span> xx=<span class="built_in">upper_bound</span>(r[x].<span class="built_in">begin</span>(),r[x].<span class="built_in">end</span>(),y);</span><br><span class="line">        <span class="keyword">if</span>(xx!=r[x].<span class="built_in">begin</span>()) rr=*(xx<span class="number">-1</span>);</span><br><span class="line">        <span class="keyword">auto</span> yy=<span class="built_in">lower_bound</span>(l[y].<span class="built_in">begin</span>(),l[y].<span class="built_in">end</span>(),x);</span><br><span class="line">        <span class="keyword">if</span>(yy!=l[y].<span class="built_in">end</span>()) ll=*yy;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span>(ll!=inf&amp;&amp;rr!=<span class="number">0</span>&amp;&amp;rr&gt;=ll<span class="number">-1</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(ll==x&amp;&amp;rr==y)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span>(mp[&#123;ll,rr&#125;]&gt;=<span class="number">2</span>) f=<span class="number">1</span>;</span><br><span class="line">                <span class="keyword">else</span> <span class="keyword">if</span>(R[ll<span class="number">+1</span>]&lt;rr) f=<span class="number">1</span>;</span><br><span class="line">                <span class="keyword">else</span> <span class="keyword">if</span>(!r[ll].<span class="built_in">empty</span>()&amp;&amp;r[ll].<span class="built_in">front</span>()&lt;rr) f=<span class="number">1</span>;</span><br><span class="line">                <span class="keyword">else</span> <span class="keyword">if</span>(!l[rr].<span class="built_in">empty</span>()&amp;&amp;l[rr].<span class="built_in">back</span>()&gt;ll) f=<span class="number">1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> f=<span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(f) cout&lt;&lt;<span class="string">&quot;Yes\n&quot;</span>;</span><br><span class="line">        <span class="keyword">else</span> cout&lt;&lt;<span class="string">&quot;No\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="F">F</h1>
<h3 id="题意-v6">题意</h3>
<p>给定一个长度为 N−1 的序列 D，构造一个  P，使得对于每一个 1≤i≤N−1，在后缀 Pi…PN 中，最大值和次大值的下标差的绝对值等于 Di，求满足条件的P数量。</p>
<h3 id="思路-v6">思路</h3>
<p>我们可以从后往前推，相当于每次在前面加一个Pi
假设在后缀Pi+1...PN中,最大值下标为x，次大值下标为y，|x-y|=Di+1
现在我们加入Pi，有3种可能性
1.Pi变成最大值，那次大值下标就是x,那么|i-x|=Di
2.Pi变成次大值，那最大值不变还是x,那么|x-i|=Di
3.Pi不是最大值也不是次大值，此时因为x，y不变，推出Di=Di+1
Pi的取值可以在N-i-1中选择</p>
<p>由上面3种情况可以推出状态其实和次大值的位置没有关系，我们假设f[i][j]
表示在后缀Pi...PN中，最大值的下标为j的数量
状态转移
如果Di=Di+1
f[i][j]+=f[i+1][j]×(N−i−1)  （对应3）
如果j-i=Di
f[i][i]+=f[i+1][j] （对应1）
f[i][j]+=f[i+1][j]   (对应2)
但是现在的时间复杂度是O(N2) TLE
我们需要优化！！！
可以使用线段树优化
1.区间乘法：当 D[i] = D[i+1] 时，将区间 [i+1, n] 的值全部乘以 (n - i - 1)
2.单点修改：处理2，3的时候，更新f[i]
3.单点查询: 获取f[i]</p>
<h3 id="代码-v6">代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">2e5</span><span class="number">+5</span>,mod=<span class="number">998244353</span>;</span><br><span class="line"><span class="meta">#<span class="keyword">define</span> ls (p&lt;&lt;1)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> rs (p&lt;&lt;1 |1)</span></span><br><span class="line">ll tree[N&lt;&lt;<span class="number">2</span>],tag[N&lt;&lt;<span class="number">2</span>];</span><br><span class="line"><span class="type">int</span> n;</span><br><span class="line"><span class="type">int</span> a[N];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">pushup</span><span class="params">(<span class="type">int</span> p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    tree[p]=(tree[ls]+tree[rs])%mod;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">pushmul</span><span class="params">(<span class="type">int</span> p,ll x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    tree[p]=tree[p]*x%mod;</span><br><span class="line">    tag[p]=tag[p]*x%mod;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">pushdown</span><span class="params">(<span class="type">int</span> p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(tag[p]==<span class="number">1</span>) <span class="keyword">return</span> ;</span><br><span class="line">    <span class="built_in">pushmul</span>(ls,tag[p]);</span><br><span class="line">    <span class="built_in">pushmul</span>(rs,tag[p]);</span><br><span class="line">    tag[p]=<span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">build</span><span class="params">(<span class="type">int</span> p,<span class="type">int</span> l,<span class="type">int</span> r)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    tree[p]=<span class="number">0</span>;</span><br><span class="line">    tag[p]=<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span>(l==r)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> mid=(l+r)&gt;&gt;<span class="number">1</span>;</span><br><span class="line">    <span class="built_in">build</span>(ls,l,mid);</span><br><span class="line">    <span class="built_in">build</span>(rs,mid<span class="number">+1</span>,r);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function">ll <span class="title">check</span><span class="params">(<span class="type">int</span> p,<span class="type">int</span> k,<span class="type">int</span> l,<span class="type">int</span> r)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(l==r)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> tree[p];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">pushdown</span>(p);</span><br><span class="line">    <span class="type">int</span> mid=(l+r)&gt;&gt;<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span>(k&lt;=mid) <span class="keyword">return</span> <span class="built_in">check</span>(ls,k,l,mid);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> <span class="built_in">check</span>(rs,k,mid<span class="number">+1</span>,r);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">xgmul</span><span class="params">(<span class="type">int</span> p,<span class="type">int</span> l,<span class="type">int</span> r,<span class="type">int</span> L,<span class="type">int</span> R,ll x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(L&lt;=l&amp;&amp;r&lt;=R)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">pushmul</span>(p,x);</span><br><span class="line">	    <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">pushdown</span>(p);</span><br><span class="line">    <span class="type">int</span> mid=(l+r)&gt;&gt;<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span>(L&lt;=mid) <span class="built_in">xgmul</span>(ls,l,mid,L,R,x);</span><br><span class="line">    <span class="keyword">if</span>(R&gt;mid) <span class="built_in">xgmul</span>(rs,mid<span class="number">+1</span>,r,L,R,x);</span><br><span class="line">    <span class="built_in">pushup</span>(p);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">xgadd</span><span class="params">(<span class="type">int</span> p,<span class="type">int</span> k,<span class="type">int</span> l,<span class="type">int</span> r,ll x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(l==r)</span><br><span class="line">    &#123;</span><br><span class="line">        tree[p]+=x;</span><br><span class="line">        tree[p]%=mod;</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">pushdown</span>(p);</span><br><span class="line">    <span class="type">int</span> mid=(l+r)&gt;&gt;<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span>(k&lt;=mid) <span class="built_in">xgadd</span>(ls,k,l,mid,x);</span><br><span class="line">    <span class="keyword">else</span> <span class="built_in">xgadd</span>(rs,k,mid<span class="number">+1</span>,r,x);</span><br><span class="line">    <span class="built_in">pushup</span>(p);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   cin&gt;&gt;n;</span><br><span class="line">   <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;n;++i)</span><br><span class="line">   &#123;</span><br><span class="line">        cin&gt;&gt;a[i];</span><br><span class="line">   &#125;</span><br><span class="line">   a[n]=<span class="number">0</span>;</span><br><span class="line">   <span class="keyword">if</span>(a[n<span class="number">-1</span>]!=<span class="number">1</span>)</span><br><span class="line">   &#123;</span><br><span class="line">        cout&lt;&lt;<span class="string">&quot;0\n&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="built_in">build</span>(<span class="number">1</span>,<span class="number">1</span>,n);</span><br><span class="line">   <span class="built_in">xgadd</span>(<span class="number">1</span>,n,<span class="number">1</span>,n,<span class="number">1</span>);</span><br><span class="line">   <span class="built_in">xgadd</span>(<span class="number">1</span>,n<span class="number">-1</span>,<span class="number">1</span>,n,<span class="number">1</span>);</span><br><span class="line">   <span class="keyword">for</span>(<span class="type">int</span> i=n<span class="number">-2</span>;i&gt;=<span class="number">1</span>;--i)</span><br><span class="line">   &#123;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> j=a[i]+i;</span><br><span class="line">        ll v=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">if</span>(j&lt;=n) v=<span class="built_in">check</span>(<span class="number">1</span>,j,<span class="number">1</span>,n);</span><br><span class="line">        <span class="keyword">if</span>(a[i]==a[i<span class="number">+1</span>])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">xgmul</span>(<span class="number">1</span>,<span class="number">1</span>,n,<span class="number">1</span>,n,n-i<span class="number">-1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="built_in">xgmul</span>(<span class="number">1</span>,<span class="number">1</span>,n,<span class="number">1</span>,n,<span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span>(v)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">xgadd</span>(<span class="number">1</span>,j,<span class="number">1</span>,n,v);</span><br><span class="line">            <span class="built_in">xgadd</span>(<span class="number">1</span>,i,<span class="number">1</span>,n,v);</span><br><span class="line">        &#125;    </span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;tree[<span class="number">1</span>]%mod&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="G">G</h1>
<h3 id="题意-v7">题意</h3>
<p>有 N个苹果，第 i个苹果会在时间 Ti掉落在 Xi 处，可以在任意位置放机器人，机器人从 0 时刻开始移动，速度不大于1，每个机器人同一时刻只能吃一个苹果，求最少需要多少个机器人，才能吃完全部苹果。</p>
<h3 id="思路-v7">思路</h3>
<p>假设一个机器人在Ti的时候吃了Xi位置上的苹果，现在要去Tj时间Xi位置上的苹果，由于速度&lt;=1，
可以推出∣Xi​−Xj​∣&lt;=Tj​−Ti​
拆开整理一下得出
1.Ti​+Xi​&lt;=Tj​+Xj​
2.Ti​−Xi​&lt;=Tj​−Xj​
然后我们令  A=T-X，B=T+X
得出   Ai&lt;=Aj &amp;&amp;Bi&lt;=Bj​ 的时候，机器人可以从i到j吃到苹果
题目问我们最少需要但是机器人才可以吃完苹果 那么就变成了求最长严格递减子序列的问题
我们需要先对A排序，然后对B进行求解</p>
<h3 id="代码-v7">代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">3e5</span><span class="number">+5</span>;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">node</span>&#123;</span><br><span class="line">    <span class="type">int</span> x,y;</span><br><span class="line">&#125;;</span><br><span class="line">node a[N];</span><br><span class="line"><span class="type">int</span> f[N];</span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">cmp</span><span class="params">(node a,node b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(a.x!=b.x) <span class="keyword">return</span> a.x&lt;b.x;</span><br><span class="line">    <span class="keyword">return</span> a.y&lt;b.y; </span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> n;</span><br><span class="line">    cin&gt;&gt;n;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;n;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> t,x;</span><br><span class="line">        cin&gt;&gt;t&gt;&gt;x;</span><br><span class="line">        a[i]=&#123;t-x,t+x&#125;;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">sort</span>(a,a+n,cmp);</span><br><span class="line">    <span class="type">int</span> cnt=<span class="number">0</span>;</span><br><span class="line">    f[cnt++]=a[<span class="number">0</span>].y;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;n;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(a[i].y&lt;f[cnt<span class="number">-1</span>])</span><br><span class="line">        &#123;</span><br><span class="line">            f[cnt++]=a[i].y;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="type">int</span> l=<span class="number">0</span>,r=cnt<span class="number">-1</span>,pos=<span class="number">-1</span>;</span><br><span class="line">            <span class="keyword">while</span>(l&lt;=r)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="type">int</span> mid=(l+r)&gt;&gt;<span class="number">1</span>;</span><br><span class="line">                <span class="keyword">if</span>(f[mid]&lt;a[i].y)</span><br><span class="line">                &#123;</span><br><span class="line">                    pos=mid;</span><br><span class="line">                    r=mid<span class="number">-1</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> <span class="keyword">if</span>(f[mid]==a[i].y)</span><br><span class="line">                &#123;</span><br><span class="line">                    pos=<span class="number">-1</span>;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> l=mid<span class="number">+1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(pos!=<span class="number">-1</span>) f[pos]=a[i].y;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;cnt&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>算法</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>图论</title>
    <url>/2026/05/18/%E5%9B%BE%E8%AE%BA/</url>
    <content><![CDATA[<h1 id="图的储存">图的储存</h1>
<h2 id="1-邻接矩阵">1.邻接矩阵</h2>
<p>使用条件：适合点数比较少的,注意处理重边时需取 $\min$。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> MAXN = <span class="number">1005</span>; <span class="comment">// 最大顶点数</span></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> INF = <span class="number">0x3f3f3f3f</span>; <span class="comment">// 无穷大</span></span><br><span class="line"><span class="type">int</span> g[MAXN][MAXN]; <span class="comment">// g[u][v] 存储 u 到 v 的边权</span></span><br><span class="line"><span class="type">int</span> n, m; <span class="comment">// n个点，m条边</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; m; i++) &#123;</span><br><span class="line">        <span class="type">int</span> u, v, w;</span><br><span class="line">        cin &gt;&gt; u &gt;&gt; v &gt;&gt; w;</span><br><span class="line">        g[u][v]=<span class="built_in">min</span>( g[u][v], w ); <span class="comment">// 防止重边，取最小权</span></span><br><span class="line">	    g[v][u]=<span class="built_in">min</span>( g[v][u], w );<span class="comment">// 如果是无向图，加上这一行</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><a href="https://www.luogu.com.cn/problem/P2245">P2245 星际导航 - 洛谷</a></p>
<h2 id="2-邻接表">2.邻接表</h2>
<p>适合大部分场景，简单易懂</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123; </span><br><span class="line">	<span class="type">int</span> v;ll w;</span><br><span class="line">&#125;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N = <span class="number">100005</span>; <span class="comment">// 节点数量</span></span><br><span class="line">vector&lt;Edge&gt; adj[N];   <span class="comment">// 邻接表</span></span><br><span class="line"><span class="comment">// 加边函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">add_edge</span><span class="params">(<span class="type">int</span> u, <span class="type">int</span> v,<span class="type">int</span> w)</span> </span>&#123;</span><br><span class="line">    adj[u].<span class="built_in">push_back</span>(&#123;v,w&#125;);</span><br><span class="line">    adj[v].<span class="built_in">push_back</span>(&#123;u,w&#125;); <span class="comment">// 如果是无向图，两边都要加</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 遍历</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">auto</span> [v,w]:adj[u])</span><br><span class="line">&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p><strong>Trick</strong>：$O(1)$ <strong>取反向边？</strong> 在网络流或一些无向图博弈问题中，我们常常需要通过一条有向边 $u \to v$ 快速找到对应的反向边 $v \to u$。
实现：<strong>在结构体中额外记录一个 <code>rev</code> 变量，存储反向边在目标节点 <code>vector</code> 中的下标</strong>。</p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123; </span><br><span class="line">	<span class="type">int</span> v;ll w;</span><br><span class="line">	<span class="type">int</span> rev;</span><br><span class="line">&#125;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N = <span class="number">100005</span>; <span class="comment">// 节点数量</span></span><br><span class="line">vector&lt;Edge&gt; adj[N];   <span class="comment">// 邻接表</span></span><br><span class="line"><span class="comment">// 加边函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">add_edge</span><span class="params">(<span class="type">int</span> u, <span class="type">int</span> v,<span class="type">int</span> w)</span> </span>&#123;</span><br><span class="line">    adj[u].<span class="built_in">push_back</span>(&#123;v,w,(<span class="type">int</span>)adj[v].<span class="built_in">size</span>()&#125;);</span><br><span class="line">    adj[v].<span class="built_in">push_back</span>(&#123;u,w,(<span class="type">int</span>)adj[u].<span class="built_in">size</span>()<span class="number">-1</span>&#125;); <span class="comment">// 如果是无向图，两边都要加</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 遍历与反向边修改演示 ( 以网络流增广为例 )</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">update_residual</span><span class="params">( <span class="type">int</span> u, <span class="type">int</span> edge_idx, ll flow )</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span>&amp; [v, w, rev] = adj[u][edge_idx];</span><br><span class="line">    w -= flow;                  <span class="comment">// 正向边流量减</span></span><br><span class="line">    adj[v][rev].w += flow;      <span class="comment">// 反向边流量加 (通过 rev 实现 O(1) 索引)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="3-链式前向星">3.链式前向星</h2>
<p>揪葡萄
想象一下，每个节点（比如节点 1）手里都只能攥住<strong>一根绳子</strong>。</p>
<ul>
<li><strong><code>head[u]</code></strong>：就是节点 u 手里攥着的那根绳子的<strong>编号</strong>。</li>
<li><strong><code>next</code></strong>：每一条边（葡萄）下面都挂着<strong>下一条边（葡萄）的编号</strong>。
当我们想找节点 1 连接的所有边时，我们先看它手里攥着哪根（<code>head[1]</code>），顺着这根绳子往下找，直到绳子断了（<code>-1</code>）。</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> maxn = <span class="number">1e5</span> + <span class="number">5</span>;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> maxm = <span class="number">2e5</span> + <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span> &#123;</span><br><span class="line">    <span class="type">int</span> v, nxt; ll w;</span><br><span class="line">    <span class="comment">// 目标 | 下一条边 | 边权</span></span><br><span class="line">&#125; edge[maxm];</span><br><span class="line"><span class="type">int</span> head[maxn], tot;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">init</span><span class="params">( )</span> </span>&#123;</span><br><span class="line">    <span class="built_in">memset</span>( head, <span class="number">-1</span>, <span class="keyword">sizeof</span> head );</span><br><span class="line">    tot = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">add_edge</span><span class="params">( <span class="type">int</span> u, <span class="type">int</span> v, ll w )</span> </span>&#123;</span><br><span class="line">    edge[tot] = &#123; v, w, head[u] &#125;;</span><br><span class="line">    head[u] = tot ++;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">search</span><span class="params">( <span class="type">int</span> u )</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span>( <span class="type">int</span> i = head[u]; i != <span class="number">-1</span>; i = edge[i].nxt ) &#123;</span><br><span class="line">        <span class="type">int</span> v = edge[i].v; ll w = edge[i].w;</span><br><span class="line">        cout &lt;&lt; u &lt;&lt; <span class="string">&quot; -&gt; &quot;</span> &lt;&lt; v &lt;&lt; <span class="string">&quot; (&quot;</span> &lt;&lt; w &lt;&lt; <span class="string">&quot;)&quot;</span> &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 插入一对正反向边（网络流初始化）</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">add_flow_edge</span><span class="params">( <span class="type">int</span> u, <span class="type">int</span> v, ll cap )</span> </span>&#123;</span><br><span class="line">    <span class="built_in">add_edge</span>( u, v, cap ); <span class="comment">// 正向边，编号为 cnt</span></span><br><span class="line">    <span class="built_in">add_edge</span>( v, u, <span class="number">0</span> );   <span class="comment">// 反向边，编号为 cnt ^ 1</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 残量网络更新演示 (不再需要额外的 rev 变量)</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">update_residual</span><span class="params">( <span class="type">int</span> edge_idx, ll flow )</span> </span>&#123;</span><br><span class="line">    edge[edge_idx].w -= flow;     <span class="comment">// 正向边减流量</span></span><br><span class="line">    edge[edge_idx ^ <span class="number">1</span>].w += flow; <span class="comment">// 反向边加流量 (通过异或 O(1) 直接定位)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="4-只有边的数组-Kruskal-算法时">4.只有边的数组(Kruskal 算法时)</h2>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span> &#123;</span><br><span class="line">    <span class="type">int</span> u, v, w;</span><br><span class="line">    <span class="comment">// 重载 &lt; 运算符，方便 sort 排序</span></span><br><span class="line">    <span class="type">bool</span> <span class="keyword">operator</span>&lt;(<span class="type">const</span> Edge&amp; other) <span class="type">const</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> w &lt; other.w;</span><br><span class="line">    &#125;</span><br><span class="line">&#125; ;</span><br><span class="line"><span class="comment">// 使用数组存储  </span></span><br><span class="line">Edge edges[MAXM];</span><br><span class="line"><span class="type">int</span> edge_count = <span class="number">0</span>; </span><br><span class="line"><span class="comment">// 加边函数 </span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">add_edge</span><span class="params">(<span class="type">int</span> u, <span class="type">int</span> v, <span class="type">int</span> w)</span> </span></span><br><span class="line"><span class="function"></span>&#123; edges[edge_count++] = &#123;u, v, w&#125;; &#125;</span><br><span class="line"><span class="comment">// 使用时直接 sort(edges, edges + m);</span></span><br></pre></td></tr></table></figure>
<h2 id="对比">对比</h2>
<table>
<thead>
<tr>
<th>方式</th>
<th>空间</th>
<th>遍历效率</th>
<th>查边效率 $u \to v$</th>
<th>适用场景</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>直接存边</strong></td>
<td>$O(E)$</td>
<td>$O(E)$</td>
<td>$O(E)$</td>
<td>无固定遍历起点，需对整体边集进行排序与遍历（如 Kruskal 算法）。</td>
</tr>
<tr>
<td><strong>邻接矩阵</strong></td>
<td>$O(V^2)$</td>
<td>$O(V)$</td>
<td>$O(1)$</td>
<td>点数少 ($N \le 500$)，边稠密，需频繁判断连通。</td>
</tr>
<tr>
<td><strong>邻接表</strong></td>
<td>$O(E)$</td>
<td>$O(deg(u))$</td>
<td>$O(deg(u))$</td>
<td>大部分情况通用，代码最简洁，易于拓展。</td>
</tr>
<tr>
<td><strong>链式前向星</strong></td>
<td>$O(E)$</td>
<td>$O(deg(u))$</td>
<td>$O(deg(u))$</td>
<td>常数极小，方便维护反向边、边编号。</td>
</tr>
</tbody>
</table>
<h1 id="图的遍历">图的遍历</h1>
<p>这里代码都使用<strong>邻接表</strong></p>
<h2 id="DFS">DFS</h2>
<p>依赖递归实现，直到无路可走再回溯</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">vector&lt;<span class="type">int</span>&gt; adj[N]; <span class="comment">// 邻接表</span></span><br><span class="line">vector&lt;<span class="type">bool</span>&gt; vis;         <span class="comment">// 记录节点是否已经遍历</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> u)</span> </span>&#123;</span><br><span class="line">	vis[u] = <span class="literal">true</span>;</span><br><span class="line">	cout&lt;&lt;u&lt;&lt;<span class="string">&quot; &quot;</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> v : adj[u])</span><br><span class="line">	<span class="keyword">if</span> (!vis[v]) <span class="built_in">dfs</span>(v)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="BFS">BFS</h2>
<p>依赖队列实现，一层一层搜索</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">vector&lt;<span class="type">int</span>&gt; adj[N];   <span class="comment">// 邻接表</span></span><br><span class="line">vector&lt;<span class="type">bool</span>&gt; vis;         <span class="comment">// 记录节点是否已经遍历</span></span><br><span class="line"><span class="type">int</span> n;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">bfs</span><span class="params">(<span class="type">int</span> s)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    queue&lt;<span class="type">int</span>&gt; q;</span><br><span class="line">    vis[s] = <span class="literal">true</span>;</span><br><span class="line">    q.<span class="built_in">push</span>(s);</span><br><span class="line">    <span class="keyword">while</span> (!q.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        <span class="type">int</span> u = q.<span class="built_in">front</span>(); q.<span class="built_in">pop</span>();</span><br><span class="line">        cout &lt;&lt; u &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> v : adj[u]) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!vis[v]) &#123;</span><br><span class="line">                vis[v] = <span class="literal">true</span>;</span><br><span class="line">                q.<span class="built_in">push</span>(v);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="拓扑排序">拓扑排序</h1>
<p>特点：使得对于图中的任意一条有向边 <code>(u -&gt; v)</code>，在排序结果中 u 总是出现在 v 的前面
时间复杂度：$O(V+E)$
<strong>注意</strong>：1.拓扑排序的结果<strong>不一定唯一</strong>，只要有多个顶点的入度同时为 0，谁先被访问都可以
2.有环无法进行拓扑排序
3.它不是传统意义上的数值排序，而是一种<strong>偏序关系</strong>的线性化</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> ind[N],n;<span class="comment">//ind是入度的数量</span></span><br><span class="line">vector &lt;<span class="type">int</span>&gt; adj[N],tp;</span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">topo</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	queue&lt;<span class="type">int</span>&gt; q;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">if</span>(ind[i]==<span class="number">0</span>) q.<span class="built_in">push</span>(i);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">while</span>(!q.<span class="built_in">empty</span>())</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> u=q.<span class="built_in">front</span>();</span><br><span class="line">		q.<span class="built_in">pop</span>();</span><br><span class="line">		<span class="comment">//记录</span></span><br><span class="line">		tp.<span class="built_in">push_back</span>(u);</span><br><span class="line">		<span class="keyword">for</span>(<span class="type">int</span> v:adj[u])</span><br><span class="line">		&#123;</span><br><span class="line">			ind[v]--;</span><br><span class="line">			<span class="keyword">if</span>(ind[v]==<span class="number">0</span>) q.<span class="built_in">push</span>(v);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> tp.<span class="built_in">size</span>()==n;	</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//输出排序</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">auto</span> x:tp) <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>,x);</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h1 id="最小生成树">最小生成树</h1>
<h2 id="1-Prim">1.Prim</h2>
<h3 id="朴素版">朴素版</h3>
<p>时间复杂度：$O(V^2)$
<strong>核心操作</strong>：每次都要遍历所有顶点，去寻找距离集合最近的那个未访问顶点</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> INF = <span class="number">0x3f3f3f3f</span>;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span> &#123; </span><br><span class="line">	<span class="type">int</span> v; ll w; </span><br><span class="line">&#125;;</span><br><span class="line">vector&lt;Edge&gt; adj[N]; <span class="comment">// 邻接表</span></span><br><span class="line"><span class="type">int</span> dis[N];       <span class="comment">// 点到生成树的距离</span></span><br><span class="line"><span class="type">bool</span> vis[N];        <span class="comment">// 是否已在树中</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">prim</span><span class="params">(<span class="type">int</span> V)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">memset</span>(dis, <span class="number">0x3f</span>, <span class="keyword">sizeof</span> dis);</span><br><span class="line">    dis[<span class="number">1</span>] = <span class="number">0</span>;</span><br><span class="line">    ll ans = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> cnt = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;=V; i++) &#123;</span><br><span class="line">        <span class="type">int</span> t = <span class="number">-1</span>;</span><br><span class="line">        <span class="comment">// 1. 暴力遍历：找当前不在树中且距离树最近的点 t</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">1</span>; j &lt;= V; j++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!vis[j] &amp;&amp; (t == <span class="number">-1</span> || dis[t] &gt; dis[j])) &#123;</span><br><span class="line">                t = j;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 检查连通性</span></span><br><span class="line">        <span class="keyword">if</span> (t==<span class="number">-1</span>||dis[t] == INF) <span class="keyword">return</span> INF;</span><br><span class="line">        <span class="comment">// 2. 统计结果</span></span><br><span class="line">        ans += dis[t];</span><br><span class="line">        vis[t] = <span class="literal">true</span>;</span><br><span class="line">        cnt++;</span><br><span class="line">        <span class="comment">// 3. 用新加入的点 t 更新它的邻居到树的距离</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">auto</span> [v,w]: adj[t]) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!vis[v] &amp;&amp; dis[v] &gt; w) &#123;</span><br><span class="line">                dis[v] = w;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> (cnt == V) ? ans : <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="堆优化版">堆优化版</h3>
<p>时间复杂度：$O(Elog V)$
<strong>核心操作</strong>：把所有可用的边扔进一个最小堆（优先队列)中,每次直接从堆顶弹出当前权值最小的边，取出顶点</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> INF = <span class="number">0x3f3f3f3f</span>;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span> &#123;</span><br><span class="line">    <span class="type">int</span> v; ll w;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">State</span> &#123;</span><br><span class="line">    <span class="type">int</span> u; ll d;</span><br><span class="line">    <span class="comment">// 优先队列维护小根堆</span></span><br><span class="line">    <span class="type">bool</span> <span class="keyword">operator</span> &lt; (<span class="type">const</span> State&amp; oth) <span class="type">const</span> &#123; <span class="keyword">return</span> d &gt; oth.d; &#125;</span><br><span class="line">&#125;;</span><br><span class="line">vector&lt;Edge&gt; adj[N]; <span class="comment">// 邻接表</span></span><br><span class="line">ll dis[N];       <span class="comment">// 点到生成树的距</span></span><br><span class="line"><span class="type">bool</span> vis[N];        <span class="comment">// 是否已在树中</span></span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">prim</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">fill</span>( dis + <span class="number">1</span>, dis + V + <span class="number">1</span>, inf );</span><br><span class="line">    <span class="built_in">fill</span>( vis + <span class="number">1</span>, vis + V + <span class="number">1</span>, <span class="literal">false</span> );</span><br><span class="line">    priority_queue&lt;State&gt; pq;</span><br><span class="line">    dis[<span class="number">1</span>] = <span class="number">0</span>;</span><br><span class="line">    q.<span class="built_in">push</span>(&#123;<span class="number">1</span>,<span class="number">0</span>&#125;);</span><br><span class="line">    ll ans = <span class="number">0</span>; <span class="comment">// 总权重</span></span><br><span class="line">    <span class="type">int</span> cnt = <span class="number">0</span>; <span class="comment">// 进树的点数</span></span><br><span class="line">    <span class="keyword">while</span> (!pq.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        <span class="keyword">auto</span> [u,d]=pq.<span class="built_in">top</span>();</span><br><span class="line">        pq.<span class="built_in">pop</span>();</span><br><span class="line">        <span class="keyword">if</span> (vis[u]) <span class="keyword">continue</span>; <span class="comment">// 冗余点处理</span></span><br><span class="line">        vis[u] = <span class="literal">true</span>;</span><br><span class="line">        ans += d;</span><br><span class="line">        cnt++;</span><br><span class="line">        <span class="comment">// 遍历 u 的所有邻居</span></span><br><span class="line">        <span class="keyword">for</span>( <span class="keyword">auto</span> [v, w] : adj[u] ) &#123;</span><br><span class="line">            <span class="keyword">if</span>( !vis[v] &amp;&amp; w &lt; dis[v] ) &#123;</span><br><span class="line">                dis[v] = w;</span><br><span class="line">                pq.<span class="built_in">push</span>(&#123; v, dis[v]&#125;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> (cnt == n) ? ans : INF;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="2-Kruskal">2.Kruskal</h2>
<p>时间复杂度：$O(ElogE)$
<strong>核心操作</strong>:使用<strong>并查集</strong>，对于每一条边，判断其两端点是否连通。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义边的结构体</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span> &#123;</span><br><span class="line">    <span class="type">int</span> u, v;ll w;</span><br><span class="line">    <span class="comment">// 重载 &lt; 运算符，用于 sort(也可以写一个cmp比较函数)</span></span><br><span class="line">    <span class="type">bool</span> <span class="keyword">operator</span>&lt;(<span class="type">const</span> Edge&amp; other) <span class="type">const</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> w &lt; other.w;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;edg[N];</span><br><span class="line"><span class="type">int</span> fa[N],ans,cnt;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(fa[x]==x) <span class="keyword">return</span> x;</span><br><span class="line">	<span class="keyword">return</span> fa[x]=<span class="built_in">find</span>(fa[x]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function">ll <span class="title">kruskal</span><span class="params">(<span class="type">int</span> V)</span></span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="built_in">sort</span>(edg,edg+m);<span class="comment">//m是边数，n是点数</span></span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		fa[i]=i;</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="keyword">for</span>( <span class="keyword">auto</span> [u, v, w] : edg ) &#123;</span><br><span class="line">        <span class="type">int</span> ru = <span class="built_in">find</span>( u ), rv = <span class="built_in">find</span>( v );</span><br><span class="line">        <span class="keyword">if</span>( ru != rv ) &#123;</span><br><span class="line">            fa[ru] = rv;</span><br><span class="line">            ans += w; cnt ++;</span><br><span class="line">            <span class="comment">// 若需要构建 MST 的树结构，可在此处连边: adj[u].push_back(&#123;v, w&#125;);</span></span><br><span class="line">            <span class="keyword">if</span>( cnt == V - <span class="number">1</span> ) <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span>( cnt &lt; V - <span class="number">1</span> ) <span class="keyword">return</span> <span class="number">-1</span>; <span class="comment">// 图不连通，无法构成生成树</span></span><br><span class="line">    <span class="keyword">return</span> ans;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="虚拟源点">虚拟源点</h2>
<p>常见题面线索：</p>
<ul>
<li>有若干台机器/基站/集群，彼此连线有代价 <code>w(u,v)</code></li>
<li>还必须把它们接到“电网/水源/上级网络/主站”，而<strong>主站本身不在原图中</strong>，或者接主站的代价不是统一的边，而是一个<strong>接入成本</strong>；
建图方式：建立一个超级源点（虚拟源点 $0$）。将所有“点权”转化为该点到超级源点 $0$ 的“边权”。然后对包括 $0$ 号点在内的 $V+1$ 个点跑一次普通的最小生成树。</li>
</ul>
<h2 id="严格次小生成树">严格次小生成树</h2>
<p>算法的核心思想是“换边”：</p>
<ol>
<li>先用 Kruskal 算法求出最小生成树，记录其权值总和为 <code>sum</code>。</li>
<li>遍历每一条<strong>不在最小生成树中</strong>的非树边 $e(u, v, w)$。如果把这条边强行加到树中，树上就会形成一个环（即 $u$ 到 $v$ 的树上路径加上这条新边）。</li>
<li>为了重新变成一棵树，我们需要在这个环里<strong>删掉一条原本就在树上的边</strong>。</li>
<li>为了让新的树权值总和增加得最少（且严格大于 <code>sum</code>），我们需要在 $u$ 到 $v$ 的树上路径中，找到权值最大、且<strong>严格小于 $w$</strong> 的那条树边替换掉。
<strong>洛谷P4180</strong></li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">1e5</span><span class="number">+5</span>,M=<span class="number">3e5</span><span class="number">+5</span>,inf=<span class="number">2e9</span>;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">    <span class="type">int</span> u,v,w;</span><br><span class="line">    <span class="type">bool</span> vis;</span><br><span class="line">    <span class="type">bool</span> <span class="keyword">operator</span>&lt;(<span class="type">const</span> Edge&amp; other) <span class="type">const</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> w &lt; other.w;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge1</span>&#123;</span><br><span class="line">    <span class="type">int</span> v,w;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="type">int</span> fa[N];</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(fa[x]==x) <span class="keyword">return</span> x;</span><br><span class="line">    <span class="keyword">return</span> fa[x]=<span class="built_in">find</span>(fa[x]);</span><br><span class="line">&#125;</span><br><span class="line">vector&lt;Edge1&gt; adj[N];</span><br><span class="line"><span class="type">int</span> up[N][<span class="number">20</span>],dep[N],w1[N][<span class="number">20</span>],w2[N][<span class="number">20</span>];</span><br><span class="line">Edge edg[M];</span><br><span class="line">ll sum=<span class="number">0</span>;</span><br><span class="line"><span class="type">int</span> n,m;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">kru</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;    </span><br><span class="line">    <span class="type">int</span> cnt=<span class="number">0</span>;</span><br><span class="line">    <span class="built_in">sort</span>(edg,edg+m);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i) fa[i]=i;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">auto</span>&amp; [u,v,w,vis]=edg[i];</span><br><span class="line">        <span class="type">int</span> ru=<span class="built_in">find</span>(u),rv=<span class="built_in">find</span>(v);</span><br><span class="line">        <span class="keyword">if</span>(ru!=rv)</span><br><span class="line">        &#123;</span><br><span class="line">            fa[ru]=rv;</span><br><span class="line">            cnt++;</span><br><span class="line">            sum+=w;</span><br><span class="line">            vis=<span class="number">1</span>;</span><br><span class="line">            adj[v].<span class="built_in">push_back</span>(&#123;u,w&#125;);</span><br><span class="line">            adj[u].<span class="built_in">push_back</span>(&#123;v,w&#125;);</span><br><span class="line">            <span class="keyword">if</span>(cnt==n<span class="number">-1</span>) <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> vis=<span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> p,<span class="type">int</span> ww)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    dep[u]=dep[p]<span class="number">+1</span>;</span><br><span class="line">    up[u][<span class="number">0</span>]=p;</span><br><span class="line">    w1[u][<span class="number">0</span>]=ww,w2[u][<span class="number">0</span>]=-inf;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=<span class="number">19</span>;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        up[u][i]=up[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>];</span><br><span class="line">        w1[u][i]=<span class="built_in">max</span>(w1[u][i<span class="number">-1</span>],w1[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>]);</span><br><span class="line">        w2[u][i]=<span class="built_in">max</span>(w2[u][i<span class="number">-1</span>],w2[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>]);</span><br><span class="line">        <span class="keyword">if</span>(w1[u][i<span class="number">-1</span>]!=w1[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>])</span><br><span class="line">        &#123;</span><br><span class="line">            w2[u][i]=<span class="built_in">max</span>(w2[u][i],<span class="built_in">min</span>(w1[u][i<span class="number">-1</span>],w1[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>]));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> [vv,ww]:adj[u])</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(vv==p) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="built_in">dfs</span>(vv,u,ww);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">lca</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> v)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(dep[v]&gt;dep[u]) <span class="built_in">swap</span>(u,v);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(dep[up[u][i]]&gt;=dep[v])</span><br><span class="line">        &#123;</span><br><span class="line">            u=up[u][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(u==v)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> u;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(up[u][i]!=up[v][i])</span><br><span class="line">        &#123;</span><br><span class="line">            u=up[u][i];</span><br><span class="line">            v=up[v][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> up[u][<span class="number">0</span>];</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">F</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> v,<span class="type">int</span> w)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> res=-inf;</span><br><span class="line">    <span class="type">int</span> l=<span class="built_in">lca</span>(u,v);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(up[u][i]&amp;&amp;dep[up[u][i]]&gt;=dep[l])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(w1[u][i]&lt;w)</span><br><span class="line">            &#123;</span><br><span class="line">                res=<span class="built_in">max</span>(res,w1[u][i]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[u][i]);</span><br><span class="line">            u=up[u][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(up[v][i]&amp;&amp;dep[up[v][i]]&gt;=dep[l])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(w1[v][i]&lt;w)</span><br><span class="line">            &#123;</span><br><span class="line">                res=<span class="built_in">max</span>(res,w1[v][i]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[v][i]);</span><br><span class="line">            v=up[v][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cin&gt;&gt;n&gt;&gt;m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> x,y,z;</span><br><span class="line">        cin&gt;&gt;x&gt;&gt;y&gt;&gt;z;</span><br><span class="line">        edg[i]=&#123;x,y,z&#125;;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">kru</span>();</span><br><span class="line">    <span class="built_in">dfs</span>(<span class="number">1</span>,<span class="number">0</span>,-inf);</span><br><span class="line">    ll ans=<span class="number">1e18</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">auto</span> [u,v,w,vis]=edg[i];</span><br><span class="line">        <span class="keyword">if</span>(!vis&amp;&amp;u!=v)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> val=<span class="built_in">F</span>(u,v,w);</span><br><span class="line">            <span class="keyword">if</span>(val&gt;-inf)</span><br><span class="line">            &#123;</span><br><span class="line">                ans=<span class="built_in">min</span>(ans,sum-val+w);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;ans&lt;&lt;<span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ios::<span class="built_in">sync_with_stdio</span>(<span class="literal">false</span>);</span><br><span class="line">    cin.<span class="built_in">tie</span>(<span class="literal">nullptr</span>);</span><br><span class="line">    <span class="type">int</span> T=<span class="number">1</span>;</span><br><span class="line">    <span class="comment">//cin&gt;&gt;T;</span></span><br><span class="line">    <span class="keyword">while</span>(T--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">solve</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="最短路问题">最短路问题</h1>
<h2 id="最短路">最短路</h2>
<h3 id="1-Dijkstra">1.Dijkstra</h3>
<p>特点：<strong>单源</strong> <strong>非负边</strong></p>
<h4 id="朴素版-v2">朴素版</h4>
<p>时间复杂度：$O(V^2)$
<strong>核心操作</strong>:每一轮都需要遍历所有顶点，找到距离起点最近的一个,并更新其邻居距离</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">1e4</span><span class="number">+5</span>;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> INF = <span class="number">0x3f3f3f3f</span>;</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; adj[N];</span><br><span class="line"><span class="type">int</span> n;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dijkstra</span><span class="params">(<span class="type">int</span> st)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">dis</span><span class="params">(n + <span class="number">1</span>, INF)</span></span>;    <span class="comment">// 距离</span></span><br><span class="line">    <span class="function">vector&lt;<span class="type">bool</span>&gt; <span class="title">vis</span><span class="params">(n + <span class="number">1</span>, <span class="literal">false</span>)</span></span>; <span class="comment">// 是否已确定</span></span><br><span class="line">    dis[st] = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">        <span class="comment">// 1. 找当前未访问中距离最小的点 t</span></span><br><span class="line">        <span class="type">int</span> t = <span class="number">-1</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">1</span>; j &lt;= n; j++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!vis[j] &amp;&amp; (t == <span class="number">-1</span> || dis[j] &lt; dis[t])) &#123;</span><br><span class="line">                t = j;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 没有可达点</span></span><br><span class="line">        <span class="keyword">if</span> (dis[t] == INF) <span class="keyword">break</span>;</span><br><span class="line">        <span class="comment">// 2. 标记</span></span><br><span class="line">        vis[t] = <span class="literal">true</span>;</span><br><span class="line">        <span class="comment">// 3. 更新</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">1</span>; j &lt;= n; j++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!vis[j] &amp;&amp; adj[t][j] &lt; INF) &#123;</span><br><span class="line">                dis[j] = <span class="built_in">min</span>(dis[j], dis[t] + adj[t][j]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="堆优化版-v2">堆优化版</h4>
<p>时间复杂度：$O(ElogE)$
<strong>核心操作</strong>:使用最小堆（优先队列）存储 <code>(距离, 顶点)</code>对</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span> &#123;</span><br><span class="line">    <span class="type">int</span> v;</span><br><span class="line">    ll w;  </span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">State</span> &#123; <span class="comment">// State 状态</span></span><br><span class="line">    <span class="type">int</span> u; ll d;</span><br><span class="line">    <span class="type">bool</span> <span class="keyword">operator</span> &lt; (<span class="type">const</span> State&amp; oth) <span class="type">const</span> &#123; <span class="keyword">return</span> d &gt; oth.d; &#125;</span><br><span class="line">&#125;;</span><br><span class="line">vector&lt;Edge&gt; adj[N];</span><br><span class="line"><span class="type">int</span> n;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dijkstra_heap</span><span class="params">(<span class="type">int</span> st)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 初始化</span></span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">dis</span><span class="params">(n + <span class="number">1</span>, INF)</span></span>;</span><br><span class="line">    <span class="function">vector&lt;<span class="type">bool</span>&gt; <span class="title">vis</span><span class="params">(n + <span class="number">1</span>, <span class="literal">false</span>)</span></span>;</span><br><span class="line">    dis[st] = <span class="number">0</span>;</span><br><span class="line">    priority_queue&lt; State &gt; pq;</span><br><span class="line">    dis[S] = <span class="number">0</span>; </span><br><span class="line">    pq.<span class="built_in">push</span>(&#123; S, <span class="number">0</span> &#125;);</span><br><span class="line">    <span class="keyword">while</span>( !pq.<span class="built_in">empty</span>( ) ) &#123;</span><br><span class="line">        <span class="keyword">auto</span> [u, d] = pq.<span class="built_in">top</span>(); pq.<span class="built_in">pop</span>();</span><br><span class="line">        <span class="comment">// 剪枝：若当前的距离已经不是最优，直接跳过</span></span><br><span class="line">        <span class="keyword">if</span>(vis[u]) <span class="keyword">continue</span>;</span><br><span class="line">        vis[u]=<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span>( d &gt; dist[u] ) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">for</span>( <span class="keyword">auto</span>&amp; [v, w] : adj[u] ) &#123;</span><br><span class="line">            <span class="keyword">if</span>( dis[v] &gt; dis[u] + w ) &#123;</span><br><span class="line">                dis[v] = dis[u] + w;</span><br><span class="line">                pq.<span class="built_in">push</span>(&#123; v, dis[v] &#125;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="2-Bellman-ford">2.Bellman-ford</h3>
<p>特点：<strong>单源</strong> <strong>负边</strong>
时间复杂度：$O(VE)$
<strong>核心操作</strong>:需要对所有顶点进行松弛,每一轮都需要遍历图中的所有边，对每条边执行松弛操作</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span> &#123; <span class="type">int</span> u, v, w; &#125;;</span><br><span class="line">vector&lt; Edge &gt; edges;</span><br><span class="line">ll dis[N];</span><br><span class="line"><span class="type">int</span> n, m, s;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">bellman_ford</span><span class="params">( <span class="type">int</span> S )</span>  </span>&#123;</span><br><span class="line">    <span class="built_in">fill</span>( dist, dist + n + <span class="number">1</span>, inf );</span><br><span class="line">    dis[S] = <span class="number">0</span>;</span><br><span class="line">    <span class="type">bool</span> relaxed;</span><br><span class="line">    <span class="keyword">for</span>( <span class="type">int</span> i = <span class="number">1</span>; i &lt;= n; ++ i ) &#123;</span><br><span class="line">        relaxed = <span class="literal">false</span>;</span><br><span class="line">        <span class="keyword">for</span>( <span class="keyword">auto</span> &amp;[u, v, w] : edges ) &#123;</span><br><span class="line">            <span class="keyword">if</span>( dis[u] != inf &amp;&amp; dis[u] + w &lt; dis[v] ) &#123;</span><br><span class="line">                dis[v] = dis[u] + w;</span><br><span class="line">                relaxed = <span class="literal">true</span>;</span><br><span class="line">                <span class="keyword">if</span>( i == n ) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>( !relaxed ) <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="3-Spfa">3.Spfa</h3>
<p>特点：<strong>负</strong>  <strong>单源</strong>
Bellman-Ford 算法的<strong>队列优化版本</strong>。
时间复杂度：$O(E)$~$O(VE)$
<strong>核心操作</strong>：<strong>只有那些在前一轮被成功松弛的顶点，才有可能引起下一轮的距离更新</strong>。因此，用一个<strong>队列</strong>来维护这些“待松弛”的顶点，避免了 Bellman-Ford 那种盲目的全局遍历。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">100</span>,inf=<span class="number">0x3f3f3f3f</span>;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">	<span class="type">int</span> v;ll w;</span><br><span class="line">&#125;;</span><br><span class="line">vector &lt;Egde&gt; adj[N];</span><br><span class="line">ll dis[N];</span><br><span class="line"><span class="type">bool</span> inq[N]; <span class="comment">// 是否在队列中</span></span><br><span class="line"><span class="type">int</span> cnt[N];  <span class="comment">// 节点入队次数，用于判负环</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回值为是否存在负环</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">spfa</span><span class="params">( <span class="type">int</span> s, <span class="type">int</span> n )</span> </span>&#123;</span><br><span class="line">	<span class="built_in">fill</span>( dist + <span class="number">1</span>, dis + N + <span class="number">1</span>, inf );</span><br><span class="line">	<span class="built_in">fill</span>( cnt + <span class="number">1</span>, cnt + N + <span class="number">1</span>, <span class="number">0</span> );</span><br><span class="line">	<span class="built_in">fill</span>( inq + <span class="number">1</span>, inq + N + <span class="number">1</span>, <span class="literal">false</span> );</span><br><span class="line">    </span><br><span class="line">	queue&lt;<span class="type">int</span>&gt; q; dis[s] = <span class="number">0</span>;</span><br><span class="line">	q.<span class="built_in">push</span>(s); inq[s] = <span class="literal">true</span>;</span><br><span class="line">    </span><br><span class="line">	<span class="keyword">while</span>( !q.<span class="built_in">empty</span>() ) &#123;</span><br><span class="line">	    <span class="type">int</span> u = q.<span class="built_in">front</span>(); q.<span class="built_in">pop</span>( );</span><br><span class="line">	    inq[u] = <span class="literal">false</span>;</span><br><span class="line">        </span><br><span class="line">	    <span class="keyword">for</span>( <span class="keyword">auto</span>&amp; [v, w] : adj[u]) &#123;</span><br><span class="line">			<span class="keyword">if</span>( dis[v] &gt; dis[u] + w ) &#123;</span><br><span class="line">				dis[v] = dis[u] + w;</span><br><span class="line">				cnt[v] = cnt[u] + <span class="number">1</span>;</span><br><span class="line">				<span class="keyword">if</span>( cnt[v] &gt;= N ) <span class="keyword">return</span> <span class="literal">true</span>; <span class="comment">// 存在负环</span></span><br><span class="line">				<span class="keyword">if</span>( !inq[v] ) q.<span class="built_in">push</span>(v), inq[v] = <span class="literal">true</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>; <span class="comment">// 无负环</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="4-Floyd">4.Floyd</h3>
<p>特点： <strong>全源</strong> <strong>负边</strong>
时间复杂度：$O(V^3)$ 适合n比较小的
<strong>核心操作</strong>：从 i到 j的最短路径，要么不经过顶点 k（保持原样），要么经过顶点 k（拆分成两段：$i \to k$和$k \to j$）。<strong>本质是动态规划</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">205</span>,inf=<span class="number">0x3f3f3f3f</span>;</span><br><span class="line"><span class="type">int</span> d[N][N];<span class="comment">//d初始化为inf</span></span><br><span class="line"><span class="type">int</span> n,m;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">init</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=n;++j)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(i==j) d[i][j]=<span class="number">0</span>;</span><br><span class="line">            <span class="keyword">else</span> d[i][j]=inf;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">floyd</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> k=<span class="number">1</span>;k&lt;=n;++k)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=n;++j)</span><br><span class="line">            &#123;</span><br><span class="line">                d[i][j]=<span class="built_in">min</span>(d[i][k]+d[k][j],d[i][j]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>floyd还可以查看两点是否连通</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">//d[][]初始化为0，有边为1</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">floyd</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> k=<span class="number">1</span>;k&lt;=n;++k)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=n;++j)</span><br><span class="line">            &#123;</span><br><span class="line">                d[i][j]|=(d[i][k]&amp;d[k][j]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="差分约束">差分约束</h2>
<p>一种将<strong>代数不等式组</strong>转化为<strong>图论最短路/最长路</strong>问题的技巧
处理Xv-Xu​≤w 的不等式</p>
<ul>
<li><strong>移项</strong>：Xv​≤Xu​+w</li>
<li><strong>映射</strong>：将变量 Xv​ 看作图中的节点 v。</li>
<li><strong>建边</strong>： 上式完美契合三角形不等式（最短路松弛条件），连一条 u→v 的权值为 w 的边。
最后我们跑一遍Spfa即可，<strong>没有负环</strong>说明<strong>有解</strong></li>
</ul>
<table>
<thead>
<tr>
<th>不等式</th>
<th>建边方式</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Xv−Xu​≤w</strong>​</td>
<td>连边 u→v，权值为 w</td>
</tr>
<tr>
<td><strong>Xu−Xv​≥w</strong>​</td>
<td>连边 u→v，权值为 -w</td>
</tr>
<tr>
<td><strong>Xu−Xv​=w</strong>​</td>
<td>连边 u→v和 v→u，权值均为 w</td>
</tr>
<tr>
<td></td>
<td></td>
</tr>
</tbody>
</table>
<h2 id="次短路">次短路</h2>
<p>在一个赋权图中，路径长度仅次于最短路的路径。次短路问题通常分为两种严格定义：</p>
<ol>
<li>
<p><strong>严格次短路</strong>：路径长度严格大于最短路（$\text{长度} &gt; \text{最短路长度}$）。</p>
</li>
<li>
<p><strong>非严格次短路</strong>：路径长度大于或等于最短路，但不能是同一条路径（允许长度相等，通常用于寻找前 $K$ 条最短路）。</p>
</li>
</ol>
<p><strong>方法</strong>：
我们在经典的 Dijkstra 算法基础上，为每个节点维护<strong>两个状态</strong>：</p>
<ul>
<li>
<p><code>dis1[i]</code>：从源点到节点 $i$ 的<strong>最短距离</strong>。</p>
</li>
<li>
<p><code>dis2[i]</code>：从源点到节点 $i$ 的<strong>次短距离</strong>。</p>
</li>
</ul>
<p><strong>更新逻辑：</strong></p>
<p>当我们在松弛一条边 $u \to v$（权值为 $w$）时，尝试用 $D = dist[u] + w$ 去更新 $v$ 的状态。更新时会遇到以下三种情况：</p>
<ol>
<li>
<p><strong>$D &lt; dis1[v]$</strong>：发现了更短的最短路。原最短路退化为次短路（<code>dis2[v] = dis1[v]</code>），然后更新最短路（<code>dist1[v] = D</code>）。两者的节点都入队。</p>
</li>
<li>
<p><strong>$dis1[v] &lt; D &lt; dis2[v]$</strong>：$D$ 夹在最短路和次短路之间。直接更新次短路（<code>dis2[v] = D</code>），并将更新后的状态入队。</p>
</li>
<li>
<p><strong>$D == dis1[v]$</strong>：（针对非严格次短路）如果不要求严格大于，可以丢弃或计入计数；如果是严格次短路，则不操作。</p>
</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">dij</span><span class="params">(<span class="type">int</span> s)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">dis1</span><span class="params">(n + <span class="number">1</span>, INF)</span></span>; <span class="comment">// 最短路</span></span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">dis2</span><span class="params">(n + <span class="number">1</span>, INF)</span></span>; <span class="comment">// 次短路</span></span><br><span class="line">	priority_queue&lt;State&gt; pq;</span><br><span class="line">    dis1[s] = <span class="number">0</span>;</span><br><span class="line">    pq.<span class="built_in">push</span>(&#123;s, <span class="number">0</span>&#125;);</span><br><span class="line">    <span class="keyword">while</span> (!pq.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        <span class="keyword">auto</span> [u,d] = pq.<span class="built_in">top</span>();</span><br><span class="line">        pq.<span class="built_in">pop</span>();</span><br><span class="line">        <span class="comment">// 如果当前弹出的距离已经比已知的次短路还要大，直接剪枝</span></span><br><span class="line">        <span class="keyword">if</span> (d &gt; dis2[u]) <span class="keyword">continue</span>;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">auto</span>&amp;[v,w]: adj[u]) &#123;</span><br><span class="line">            <span class="type">int</span> nextd = d + w;</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 情况 1：可以更新最短路</span></span><br><span class="line">            <span class="keyword">if</span> (next_d &lt; dis1[v]) &#123;</span><br><span class="line">                dis2[v] = dis1[v]; <span class="comment">// 原最短路退化为次短路</span></span><br><span class="line">                dis1[v] = nextd;   <span class="comment">// 更新最短路</span></span><br><span class="line">                pq.<span class="built_in">push</span>(&#123;v, dis1[v]&#125;);</span><br><span class="line">                pq.<span class="built_in">push</span>(&#123;v, dis2[v]&#125;);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 情况 2：大于最短路，但可以更新次短路（保证严格次短路）</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (nextd &gt; dis1[v] &amp;&amp; nextd &lt; dis2[v]) &#123;</span><br><span class="line">                dis2[v] = nextd;   <span class="comment">// 更新次短路</span></span><br><span class="line">                pq.<span class="built_in">push</span>(&#123;v, dis2[v]&#125;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="分层最短路">分层最短路</h2>
<h3 id="应用场景"><strong>应用场景</strong></h3>
<p>在求最短路时，题目增加了一些动态的“<strong>硬性限制条件”或“特殊操作机会”。</strong>
比如：</p>
<ul>
<li>有 $k$ 次机会让某条边的边权变为 0（免路费）。</li>
<li>有 $k$ 次机会让某条边的边权减半（如你上一题的代码）。</li>
<li>必须经过恰好某几个特定的点，或满足某种特定的状态切换。</li>
</ul>
<h3 id="核心思想"><strong>核心思想</strong></h3>
<p>将原图复制成k+1层（或更多），每一层代表一种<strong>状态</strong>，通过在层与层之间建立特殊的<strong>转移边</strong>来模拟状态的切换。
假设原图有 $n$ 个点，$m$ 条边，你拥有 $k$ 次“特殊机会”。我们就会建立一个包含 $k+1$ 层的图：</p>
<ul>
<li><strong>第 0 层</strong>：原图的完整复制，表示<strong>一次机会都还没用</strong>的状态。</li>
<li><strong>第 1 层</strong>：原图的完整复制，表示<strong>已经用掉 1 次机会</strong>的状态。</li>
<li>……</li>
<li><strong>第 $k$ 层</strong>：原图的完整复制，表示 <strong>$k$ 次机会已经全部用完</strong>的状态。</li>
</ul>
<h3 id="边">边</h3>
<p>分层图中的边分为两种：<strong>同层边</strong> 和 <strong>跨层边</strong></p>
<h4 id="同层边（不使用特殊机会）">同层边（不使用特殊机会）</h4>
<p>在每一层内部，原图有的边，这一层也要照搬连上，而且<strong>边权不变</strong>。</p>
<ul>
<li>
<p><strong>数学表达</strong>：如果原图中 $u$ 到 $v$ 有一条权值为 $w$ 的边，那么在第 $j$ 层内部，就要连一条 <code>(u + j*n)</code> 到 <code>(v + j*n)</code>、权值为 $w$ 的边。</p>
</li>
<li>
<p><strong>物理含义</strong>：你从 $u$ 走到 $v$，但你<strong>决定不使用</strong>特殊机会。既然没用机会，你耗费的代价就是原价 $w$，并且你所处的层数（状态）不会改变。</p>
</li>
</ul>
<h4 id="跨层边（使用特殊机会）">跨层边（使用特殊机会）</h4>
<p>这是分层图的灵魂。我们要从<strong>低楼层向高楼层</strong>连一条<strong>单向斜线边</strong>。</p>
<ul>
<li>
<p><strong>数学表达</strong>：如果原图中 $u$ 到 $v$ 的边权为 $w$，且你可以通过使用一次机会让边权变成 $w'$（比如免费就是 0，减半就是 $w/2$），那么就要从第 $j$ 层的 $u$ 连一条单向指向第 $j+1$ 层的 $v$、权值为 $w'$ 的边。即 <code>(u + j*n)</code> $\to$ <code>(v + (j+1)*n)</code>。</p>
</li>
<li>
<p><strong>物理含义</strong>：你从 $u$ 走到 $v$ 的同时，<strong>决定消耗一次特殊机会</strong>。因为用了机会，这条边的代价变成了特价 $w'$；同时由于机会次数加 1，你的状态发生了改变，从第 $j$ 层“上升”到了第 $j+1$ 层</p>
</li>
</ul>
<p>示例代码</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">	<span class="type">int</span> v;ll w; </span><br><span class="line">&#125;;</span><br><span class="line"><span class="type">int</span> n,m;</span><br><span class="line">vector&lt;Edge&gt; adj[N];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	cin&gt;&gt;n&gt;&gt;m&gt;&gt;k;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> x,y,z;</span><br><span class="line">		cin&gt;&gt;x&gt;&gt;y&gt;&gt;z;</span><br><span class="line">		<span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">0</span>;j&lt;=k;++j)</span><br><span class="line">		&#123;</span><br><span class="line">			adj[x+j*n].<span class="built_in">push_back</span>(&#123;y+j*n,z&#125;);</span><br><span class="line">			adj[y+j*n].<span class="built_in">push_back</span>(&#123;x+j*n,z&#125;);</span><br><span class="line">			<span class="keyword">if</span>(j!=k)</span><br><span class="line">			&#123;</span><br><span class="line">				adj[x+j*n].<span class="built_in">push_back</span>(&#123;y+(j<span class="number">+1</span>)*n,z/<span class="number">2</span>&#125;);</span><br><span class="line">				adj[y+j*n].<span class="built_in">push_back</span>(&#123;x+(j<span class="number">+1</span>)*n,z/<span class="number">2</span>&#125;);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;	</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="type">int</span> ans=inf;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;=k;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		ans=<span class="built_in">min</span>(ans,dis[t+i*n]);</span><br><span class="line">	&#125;</span><br><span class="line">	cout&lt;&lt;ans&lt;&lt;endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="同余最短路">同余最短路</h2>
<p><strong>适用问题</strong>：给定n个可重复使用的整数，求能拼凑出的数量、无法拼凑的最大/最小数、模K余p的最小拼凑数
<strong>方法</strong>：
1.找到最小的数 x ，然后根据取余得到 （0,1,...x-1），将这些数看作点
2.然后遍历其他的数 a[i]，进行
for(int j=0;j&lt;x;++j)	
{
e[j].push_back({(j+a[i])%x,a[i]});
}
加边
3.最后进行最短路即可</p>
<p>能不能凑出数值 X？</p>
<ul>
<li>计算 r=X%amin​。</li>
<li>如果 <code>dis[r] &lt;= X</code>，说明可以凑出！因为你可以通过不断地加 amin​（这不会改变余数），把 <code>dis[r]</code>刚好提升到 X。</li>
<li>如果 <code>dis[r] == X</code>，说明 X本身就是达到该余数的最小代价。</li>
</ul>
<p><strong>洛谷P5020</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">25005</span>,inf=<span class="number">0x3f3f3f3f</span>;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">	<span class="type">int</span> v,w;</span><br><span class="line">&#125;;</span><br><span class="line">vector &lt;Edge&gt; adj[N];</span><br><span class="line"><span class="type">int</span> n,a[<span class="number">105</span>],vis[N],dis[N],x,tr[N];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">spfa</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	queue&lt;<span class="type">int</span>&gt; q;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;x;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		dis[i]=inf,vis[i]=<span class="number">0</span>,tr[i]=<span class="number">0</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	dis[<span class="number">0</span>]=<span class="number">0</span>,vis[<span class="number">0</span>]=<span class="number">1</span>;</span><br><span class="line">	q.<span class="built_in">push</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="keyword">while</span>(!q.<span class="built_in">empty</span>())</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> u=q.<span class="built_in">front</span>();</span><br><span class="line">		q.<span class="built_in">pop</span>();</span><br><span class="line">		vis[u]=<span class="number">0</span>;</span><br><span class="line">		<span class="keyword">for</span>(<span class="keyword">auto</span> ed:adj[u])</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="type">int</span> v=ed.v,w=ed.w;</span><br><span class="line">			<span class="keyword">if</span>(dis[v]&gt;w+dis[u])</span><br><span class="line">			&#123;</span><br><span class="line">				dis[v]=dis[u]+w;</span><br><span class="line">				tr[v]=w;</span><br><span class="line">				<span class="keyword">if</span>(vis[v]==<span class="number">0</span>) </span><br><span class="line">				&#123;</span><br><span class="line">					vis[v]=<span class="number">1</span>;</span><br><span class="line">					q.<span class="built_in">push</span>(v);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;<span class="keyword">else</span> <span class="keyword">if</span>(dis[v]==dis[u]+w&amp;&amp;tr[v]&gt;w)</span><br><span class="line">			&#123;</span><br><span class="line">				tr[v]=w;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	set&lt;<span class="type">int</span>&gt; st;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;x;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		st.<span class="built_in">insert</span>(tr[i]);</span><br><span class="line">	&#125;</span><br><span class="line">	cout&lt;&lt;st.<span class="built_in">size</span>()&lt;&lt;endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	ios::<span class="built_in">sync_with_stdio</span>(<span class="literal">false</span>);</span><br><span class="line">	cin.<span class="built_in">tie</span>(<span class="number">0</span>);</span><br><span class="line">	<span class="type">int</span> t;</span><br><span class="line">	cin&gt;&gt;t;</span><br><span class="line">	<span class="keyword">while</span>(t--)</span><br><span class="line">	&#123;</span><br><span class="line">		cin&gt;&gt;n;</span><br><span class="line">		<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i) cin&gt;&gt;a[i];</span><br><span class="line">		<span class="built_in">sort</span>(a<span class="number">+1</span>,a+n<span class="number">+1</span>);</span><br><span class="line">		x=a[<span class="number">1</span>];</span><br><span class="line">		<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;x;++i)</span><br><span class="line">		&#123;</span><br><span class="line">			adj[i].<span class="built_in">clear</span>();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">2</span>;i&lt;=n;++i)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">0</span>;j&lt;x;++j)</span><br><span class="line">			&#123;</span><br><span class="line">				adj[j].<span class="built_in">push_back</span>(&#123;(j+a[i])%x,a[i]&#125;);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="built_in">spfa</span>();</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="二分图">二分图</h2>
<p>通俗来说：如果能把一个图里的所有顶点<strong>分成两个独立的集合（设为 A 组和 B 组）</strong>，使得<strong>每一条边连接的两个顶点都分别属于这两个不同的集合</strong>（即 A 组内部的点没有边相连，B 组内部的点也没有边相连），那么这个图就叫二分图
<strong>性质</strong>：二分图<strong>不含奇数环</strong></p>
<h3 id="二分图判定-染色法）">二分图判定 (染色法）</h3>
<p>从任意一个点开始染黑色，那它所有相邻的点就必须染白色。以此类推，扩散全图。如果在染色的过程中，发现<strong>某个点和它相邻的点被染成了同一种颜色</strong>，说明图里有奇环，它就不是二分图。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> N = <span class="number">100005</span>;</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; adj[N]; <span class="comment">// 邻接表存图</span></span><br><span class="line"><span class="type">int</span> color[N];     <span class="comment">// 0:未染色, 1:黑色, 2:白色</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回 true 表示是二分图，false 表示不是</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">bfs_check</span><span class="params">(<span class="type">int</span> s)</span> </span>&#123;</span><br><span class="line">    queue&lt;<span class="type">int</span>&gt; q;</span><br><span class="line">    q.<span class="built_in">push</span>(s);</span><br><span class="line">    color[s] = <span class="number">1</span>; <span class="comment">// 初始点染黑色</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span>(!q.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        <span class="type">int</span> u = q.<span class="built_in">front</span>();</span><br><span class="line">        q.<span class="built_in">pop</span>();</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> v : adj[u]) &#123;</span><br><span class="line">            <span class="keyword">if</span>(color[v] == <span class="number">0</span>) &#123; <span class="comment">// 如果邻居没染色</span></span><br><span class="line">                color[v] = <span class="number">3</span> - color[u]; <span class="comment">// 1变2, 2变1 (染成相反的颜色)</span></span><br><span class="line">                q.<span class="built_in">push</span>(v);</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span>(color[v] == color[u]) &#123;</span><br><span class="line">                <span class="comment">// 如果邻居已经染了色，且和当前点颜色一样，说明冲突了（存在奇环）</span></span><br><span class="line">                <span class="keyword">return</span> <span class="literal">false</span>; </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//调用</span></span><br><span class="line"><span class="type">bool</span> is_bipartite = <span class="literal">true</span>;</span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">1</span>; i &lt;= n; ++i) &#123;</span><br><span class="line">    <span class="keyword">if</span>(color[i] == <span class="number">0</span>) &#123; <span class="comment">// 处理图不连通的情况</span></span><br><span class="line">        <span class="keyword">if</span>(!<span class="built_in">bfs_check</span>(i)) &#123;</span><br><span class="line">            is_bipartite = <span class="literal">false</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">         &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="二分图的最大匹配（匈牙利算法）">二分图的最大匹配（匈牙利算法）</h3>
<p>它的核心思想就是经典的“<strong>月老牵线，不行就拆散重组（寻找增广路）</strong>”：</p>
<ol>
<li>帮 A 组的男 1 号找 B 组的心仪对象。如果姑娘没许配人，直接牵线。</li>
<li>如果姑娘已经许配给男 2 号了，月老就会去问男 2 号：“哥们，能不能把你媳妇让出来，我重新帮你物色一个？”</li>
<li>如果男 2 号真的找到了备胎，那他就把前妻让给男 1 号。匹配数成功 $+1$。</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> maxv = <span class="number">1050</span>;</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; adj[maxv]; <span class="comment">// 左部点连接的右部点</span></span><br><span class="line"><span class="type">int</span> match[maxv];       <span class="comment">// match[i] = j 表示右部点 i 当前匹配的是左部点 j</span></span><br><span class="line"><span class="type">bool</span> vis[maxv];        <span class="comment">// 右部点在这一轮是否被访问过（避免死循环）</span></span><br><span class="line"><span class="comment">// 寻找从 u 出发的增广路</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">dfs</span><span class="params">( <span class="type">int</span> u )</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span>( <span class="type">int</span> v : adj[u] ) &#123;</span><br><span class="line">        <span class="keyword">if</span>( vis[v] ) <span class="keyword">continue</span>;</span><br><span class="line">        vis[v] = <span class="number">1</span>;</span><br><span class="line">        <span class="comment">// 如果右部点 v 还是单身，或者 v 的现任 match[v] 能够找到下家</span></span><br><span class="line">        <span class="keyword">if</span>( match[v] == <span class="number">-1</span> || <span class="built_in">dfs</span>( match[v] ) ) &#123;</span><br><span class="line">            match[v] = u;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">hungarian</span><span class="params">( <span class="type">int</span> n )</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> ans = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">memset</span>( match, <span class="number">-1</span>, <span class="built_in">sizeof</span>( match ) );</span><br><span class="line">    <span class="keyword">for</span>( <span class="type">int</span> i = <span class="number">1</span>; i &lt;= n; ++ i ) &#123;</span><br><span class="line">        <span class="built_in">memset</span>( vis, <span class="number">0</span>, <span class="built_in">sizeof</span>( vis ) ); </span><br><span class="line">        <span class="comment">// 每一轮每个左部点都要重置访问标记</span></span><br><span class="line">        <span class="keyword">if</span>( <span class="built_in">dfs</span>( i ) ) ans ++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ans;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="连通分量">连通分量</h1>
<table>
<thead>
<tr>
<th><strong>特性</strong></th>
<th><strong>强连通分量 (SCC)</strong></th>
<th><strong>弱连通分量 (WCC)</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>路径要求</strong></td>
<td>必须有<strong>双向</strong>有向路径</td>
<td>仅要求有<strong>无向</strong>连接</td>
</tr>
<tr>
<td><strong>方向敏感性</strong></td>
<td>严格遵守箭头方向</td>
<td>忽略箭头方向</td>
</tr>
<tr>
<td><strong>经典算法</strong></td>
<td><strong>Tarjan 算法</strong>、Kosaraju 算法</td>
<td>并查集 (DSU)、BFS/DFS</td>
</tr>
<tr>
<td><strong>应用场景</strong></td>
<td>寻找环、缩点转化 DAG、解决 2-SAT 问题</td>
<td>社交网络群体划分、基础连通性分析</td>
</tr>
</tbody>
</table>
<h2 id="1-强连通分量-SCC">1. 强连通分量 (SCC)</h2>
<p><strong>定义：</strong>
在一个有向图的子图中，如果任意两个顶点 $u$ 和 $v$ 之间都存在<strong>双向路径</strong>（即既有从 $u$ 到 $v$ 的路径，也有从 $v$ 到 $u$ 的路径），那么这个子图被称为是<strong>强连通</strong>的。一个图的最大强连通子图就称为强连通分量。</p>
<ul>
<li><strong>直观理解</strong>：在这个分量内部，你可以从任何一个点出发，顺着箭头的方向走到任何其他点，然后再绕回来。</li>
<li><strong>缩点</strong>：在算法竞赛中，我们常把一个 SCC 看作一个“大点”，将有向图转化成一个<strong>有向无环图 (DAG)</strong>，从而利用拓扑排序或动态规划处理。</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">vector&lt;<span class="type">int</span>&gt; adj[N];</span><br><span class="line"><span class="comment">// dfn[x] 时间戳 节点x第一次被访问的顺序</span></span><br><span class="line"><span class="comment">// low[x] 从x出发，可以访问到的最早的时间 </span></span><br><span class="line"><span class="type">int</span> dfn[N], low[N], timer;</span><br><span class="line"><span class="type">int</span> st[N], top;</span><br><span class="line"><span class="type">bool</span> inst[N];</span><br><span class="line"><span class="type">int</span> scc[N],siz[N],scc_cnt; <span class="comment">// 每个点所属的 SCC 编号</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">tarjan</span><span class="params">(<span class="type">int</span> u)</span> </span>&#123;</span><br><span class="line">	<span class="comment">// 入x时 入栈</span></span><br><span class="line">    dfn[u] = low[u] = ++timer;</span><br><span class="line">    st[++top] = u;</span><br><span class="line">    inst[u] = <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> v : adj[u]) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!dfn[v]) &#123; <span class="comment">// 未访问过</span></span><br><span class="line">            <span class="built_in">tarjan</span>(v);</span><br><span class="line">            low[u] = <span class="built_in">min</span>(low[u], low[v]);<span class="comment">// 回x时 更新low</span></span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (inst[v]) &#123; <span class="comment">// 在栈中，说明找到了环</span></span><br><span class="line">            low[u] = <span class="built_in">min</span>(low[u], dfn[v]);<span class="comment">//更新low</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 找到一个完整的 SCC</span></span><br><span class="line">    <span class="keyword">if</span> (low[u] == dfn[u]) &#123;<span class="comment">//如果x是SCC的根</span></span><br><span class="line">        scc_cnt++;</span><br><span class="line">        <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">            <span class="type">int</span> v = st[top--];</span><br><span class="line">            inst[v] = <span class="literal">false</span>;</span><br><span class="line">            scc[v] = scc_cnt;<span class="comment">//SCC的编号</span></span><br><span class="line">            ++siz[scc_cnt];<span class="comment">//SCC大小</span></span><br><span class="line">            <span class="keyword">if</span> (u == v) <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="2-弱连通分量-WCC">2. 弱连通分量 (WCC)</h2>
<p><strong>定义：</strong>
如果将有向图中的所有有向边都替换为<strong>无向边</strong>后，所得到的无向图是连通的，那么原有的有向图子图被称为<strong>弱连通</strong>的。</p>
<ul>
<li><strong>直观理解</strong>：只要忽略箭头的方向，这些点是连在一起的。就像单行道组成的城市，虽然你可能无法从 A 经过单行道回到 B，但 A 和 B 之间确实有道路相连。</li>
<li><strong>特点</strong>：强连通一定是弱连通，但弱连通不一定是强连通。</li>
</ul>
<p>处理：只需要把有向图当成无向图处理
1.遍历所有边，对于每条有向边 (u, v)，在并查集中执行合并
2.运行一次不考虑方向的 DFS</p>
<h1 id="树问题">树问题</h1>
<h2 id="二叉树">二叉树</h2>
<p>把它想象成一棵“<strong>倒过来的树</strong>”</p>
<h3 id="基础小知识">基础小知识</h3>
<ul>
<li><strong>根节点</strong>：最顶端的那个点，是所有数据的起点。</li>
<li><strong>边</strong>：连接各个点的线。</li>
<li><strong>叶子节点</strong>：最底端不再分叉的点</li>
<li><strong>节点的度</strong>：一个节点有多少个子节点（二叉树里最大就是 2）。</li>
<li><strong>叶子节点</strong>：度为 0 的节点。</li>
<li><strong>树的深度/高度</strong>：这棵树总共有多少层。</li>
<li><strong>空树</strong>：这也是一种合法的二叉树，里面一个节点都没有。</li>
</ul>
<p>特殊的二叉树
1.<strong>满二叉树</strong>：除了最后一层的叶子节点外，每一层的每个节点都有两个子节点，并且最后一层全满。
2.<strong>完全二叉树</strong>：除了最后一层，其他各层的节点数都达到了最大值；最后一层的节点全部集中在该层最左边的若干位置上。</p>
<h3 id="储存">储存</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">TreeNode</span> &#123;</span><br><span class="line">    <span class="type">int</span> val;</span><br><span class="line">    TreeNode *left;</span><br><span class="line">    TreeNode *right;</span><br><span class="line">    <span class="built_in">TreeNode</span>(<span class="type">int</span> x) : <span class="built_in">val</span>(x), <span class="built_in">left</span>(<span class="literal">NULL</span>), <span class="built_in">right</span>(<span class="literal">NULL</span>) &#123;&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h3 id="遍历">遍历</h3>
<h4 id="前序遍历">前序遍历</h4>
<p><strong>根，左，右</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">preOrder</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (root == <span class="literal">NULL</span>) <span class="keyword">return</span>; <span class="comment">// 递归基：如果是空节点，直接返回</span></span><br><span class="line">    cout &lt;&lt; root-&gt;val &lt;&lt; <span class="string">&quot; &quot;</span>; <span class="comment">// 根</span></span><br><span class="line">    <span class="built_in">preOrder</span>(root-&gt;left);     <span class="comment">// 左</span></span><br><span class="line">    <span class="built_in">preOrder</span>(root-&gt;right);    <span class="comment">// 右</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="中序遍历">中序遍历</h4>
<p><strong>左，根，右</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">inOrder</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (root == <span class="literal">NULL</span>) <span class="keyword">return</span>;</span><br><span class="line">    <span class="built_in">inOrder</span>(root-&gt;left);      <span class="comment">// 左</span></span><br><span class="line">    cout &lt;&lt; root-&gt;val &lt;&lt; <span class="string">&quot; &quot;</span>; <span class="comment">// 根</span></span><br><span class="line">    <span class="built_in">inOrder</span>(root-&gt;right);     <span class="comment">// 右</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="后序遍历">后序遍历</h4>
<p><strong>左，右，根</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">postOrder</span><span class="params">(TreeNode* root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (root == <span class="literal">NULL</span>) <span class="keyword">return</span>;</span><br><span class="line">    <span class="built_in">postOrder</span>(root-&gt;left);    <span class="comment">// 左</span></span><br><span class="line">    <span class="built_in">postOrder</span>(root-&gt;right);   <span class="comment">// 右</span></span><br><span class="line">    cout &lt;&lt; root-&gt;val &lt;&lt; <span class="string">&quot; &quot;</span>; <span class="comment">// 根</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="层序遍历">层序遍历</h4>
<p>使用queue（bfs）</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">levelOrder</span><span class="params">(TreeNode* root)</span> </span>&#123; </span><br><span class="line">	<span class="keyword">if</span> (!root) <span class="keyword">return</span>;</span><br><span class="line">	queue&lt;TreeNode*&gt; q;</span><br><span class="line">	q.<span class="built_in">push</span>(root); </span><br><span class="line">	<span class="keyword">while</span> (!q.<span class="built_in">empty</span>())</span><br><span class="line">	&#123;</span><br><span class="line">		TreeNode* curr = q.<span class="built_in">front</span>();</span><br><span class="line">		q.<span class="built_in">pop</span>();</span><br><span class="line">		cout &lt;&lt; curr-&gt;val; </span><br><span class="line">		<span class="comment">// 输出当前节点 </span></span><br><span class="line">		<span class="keyword">if</span> (curr-&gt;left)  q.<span class="built_in">push</span>(curr-&gt;left);</span><br><span class="line">		<span class="keyword">if</span> (curr-&gt;right) q.<span class="built_in">push</span>(curr-&gt;right);</span><br><span class="line">	&#125; </span><br><span class="line">	cout &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="树上差分">树上差分</h2>
<p>多次给每次给树上任意两点 u和 v之间的唯一最短路径上的所有点（或边）都加上一个值 k。
如果按常规思路，每次操作都沿着路径暴力往上爬，时间复杂度会很大，这时候我们就需要使用差分
不过在树上，我们需要借助一个关键辅助工具：<strong>最近公共祖先（LCA）</strong></p>
<h3 id="点差分">点差分</h3>
<p>我们可以把 u→v的路径拆成两段：u→l和 v→l。</p>
<ul>
<li>当你在 <code>diff[u] += k</code>时，如果你后续进行<strong>子树求和</strong>（即从下往上累加），这个值会自动一路加到根节点。同理 <code>diff[v] += k</code>也会一路往上加。</li>
<li>这样一来，l及以上的节点就被多加了两次（一次来自 u，一次来自 v）。</li>
<li>所以我们需要在 <code>diff[l] -= k</code>（消掉多算的一次），并在 <code>diff[f[l][0]] -= k</code>。</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 假设数组 d[] 为差分数组，初始全为0</span></span><br><span class="line"><span class="comment">// f[u][0] 代表 u 的父节点 (倍增法中的 parent)</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">add_node</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y, <span class="type">int</span> k)</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> l = <span class="built_in">lca</span>(x, y);</span><br><span class="line">    d[x] += k;</span><br><span class="line">    d[y] += k;</span><br><span class="line">    d[l] -= k;</span><br><span class="line">    <span class="keyword">if</span> (f[l][<span class="number">0</span>] != <span class="number">0</span>) &#123; <span class="comment">// 防止数组越界，如果是根节点的父节点</span></span><br><span class="line">        d[f[l][<span class="number">0</span>]] -= k;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//前缀和还原</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs_sum</span><span class="params">(<span class="type">int</span> u, <span class="type">int</span> p)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> v:adj[u]) &#123;</span><br><span class="line">        <span class="keyword">if</span> (v == p) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="built_in">dfs_sum</span>(v, u);</span><br><span class="line">        d[u] += d[v]; <span class="comment">// 子节点的差分值累加给父节点</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 此时 d[u] 即为该点最终的权值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>注意</strong>：
如果初始点权不为0  需要<strong>分开记录</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">//val[]初始点权 d[]为差分数组</span></span><br><span class="line"><span class="comment">// 先跑一遍标准的差分还原 DFS</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs_sum</span><span class="params">(<span class="type">int</span> u, <span class="type">int</span> p)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> v :adj[u]) &#123;</span><br><span class="line">        <span class="keyword">if</span> (v == p) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="built_in">dfs_sum</span>(v, u);</span><br><span class="line">        d[u] += d[v]; <span class="comment">// 此时 p[u] 变成了节点 u 的“总增加量”</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// ... 主函数中 ...</span></span><br><span class="line"><span class="built_in">dfs_sum</span>(<span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line"><span class="comment">// 输出时合并</span></span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">1</span>; i &lt;= n; ++i) &#123;</span><br><span class="line">    cout &lt;&lt; (val[i] + d[i]) &lt;&lt; <span class="string">&quot; &quot;</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="边差分">边差分</h3>
<ul>
<li>和刚才类似，<code>diff[u] += k</code>和 <code>diff[v] += k</code>会让 l以上的边多算两次。</li>
<li>但因为这次我们操作的是<strong>边</strong>，在 l这个拐点，左右两边的边是不连通的（总不能把 l和它父亲的边也给加了）。</li>
<li>所以我们需要在 l这里连减两次 k（一次抵消左边上来的，一次抵消右边上来的），从而把影响牢牢锁死在 u→l和 v→l这两段路径上。</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 假设数组 d[] 为差分数组</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">add_edge</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y, <span class="type">int</span> k)</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> l = <span class="built_in">lca</span>(x, y);</span><br><span class="line">    d[x] += k;</span><br><span class="line">    d[y] += k;</span><br><span class="line">    d[l] -= <span class="number">2</span> * k; </span><br><span class="line">    <span class="comment">// 注意：边差分不需要操作 f[l][0]</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//前缀和还原</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs_sum</span><span class="params">(<span class="type">int</span> u, <span class="type">int</span> p)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> v:adj[u]) &#123;</span><br><span class="line">        <span class="keyword">if</span> (v == p) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="built_in">dfs_sum</span>(v, u);</span><br><span class="line">        d[u] += d[v]; </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 此时 d[u] 代表 边(u, father) 最终的权值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="树上前缀和">树上前缀和</h2>
<p>多次给询问树上的一系列权值和，我们就需要使用树上前缀和
树上前缀和也是一样，我们需要借助一个关键辅助工具：<strong>最近公共祖先（LCA）</strong></p>
<h3 id="点前缀和">点前缀和</h3>
<p>定义 <code>s[u]</code>为 <strong>从根节点到 u的路径上所有点的权值之和</strong>。
那么路径 u→v的和为：
$$
Sum(u,v)=s[u]+s[v]−s[lca]−s[parent(lca)]
$$</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> val[N];</span><br><span class="line">ll s[N]; <span class="comment">// 根节点到 u 的前缀和</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs_pre</span><span class="params">(<span class="type">int</span> u, <span class="type">int</span> p)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> v : adj[u]) &#123;</span><br><span class="line">        <span class="keyword">if</span> (v == p) <span class="keyword">continue</span>;</span><br><span class="line">        s[v] = s[u] + val[v];</span><br><span class="line">        <span class="built_in">dfs_pre</span>(v, u);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="边前缀和">边前缀和</h3>
<p>定义 <code>s[u]</code>为 <strong>从根节点到 u的路径上所有边的权值之和</strong>。
那么路径 u→v的和为：
$$
Sum(u,v)=s[u]+se[v]−2×s[lca]
$$</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> eval[N];<span class="comment">// eval[u] 存储 u 与父节点的边权</span></span><br><span class="line">ll s[N]; <span class="comment">// 根节点到 u 的路径边权和</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs_edge</span><span class="params">(<span class="type">int</span> u, <span class="type">int</span> p)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> v : adj[u]) &#123;</span><br><span class="line">        <span class="keyword">if</span> (v == p) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="comment">// v 的父边权值是 edge_val[v]</span></span><br><span class="line">        s[v] = s[u] + eval[v];</span><br><span class="line">        <span class="built_in">dfs_edge</span>(v, u);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 主函数</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 假设 1 是根</span></span><br><span class="line">    s[<span class="number">1</span>] = <span class="number">0</span>; </span><br><span class="line">    <span class="built_in">dfs_edge</span>(<span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="最近公共祖先（LCA）">最近公共祖先（LCA）</h2>
<p>在之前提到的<strong>树上差分</strong>、<strong>路径前缀和</strong>中，LCA 都是必不可少的“中间变量”。
简单来说：<strong>LCA 就是两个节点在通往根节点的路径上，最先交汇的那个点。</strong></p>
<h3 id="1-倍增算法">1.倍增算法</h3>
<p>时间复杂度：$O(VlogV)$
<strong>核心思想</strong>：
想象你要从节点 u走到根节点。如果一步一步走太慢，我们就尝试<strong>一次跳 1,2,4,8...步</strong>。</p>
<ul>
<li><code>up[u][i]</code>表示 <strong>节点 u的第 2^i级祖先</strong>。</li>
<li>递推公式（倍增核心）：
$$
up[u][i]=up[up[u][i−1]][i−1]
$$
（意思是：u的第 <code>2^i</code>个祖先 = u的第 <code>2^i-1</code>个祖先的第 <code>2^i−1</code>个祖先）。</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">5e5</span><span class="number">+5</span>;</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; adj[N];</span><br><span class="line"><span class="type">int</span> n,m;</span><br><span class="line"><span class="type">int</span> dep[N],up[N][<span class="number">20</span>];<span class="comment">//2^20&gt;5e5+5,如果N更大,那就不是20了</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	dep[u]=dep[p]<span class="number">+1</span>;</span><br><span class="line">	<span class="comment">//向上跳1 2 4 ...步的祖先节点</span></span><br><span class="line">	up[u][<span class="number">0</span>]=p;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=<span class="number">19</span>;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		up[u][i]=up[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>];</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> v:adj[u])</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">if</span>(v!=p) <span class="built_in">dfs</span>(v,u);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">lca</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> v)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(dep[u]&lt;dep[v]) <span class="built_in">swap</span>(u,v);</span><br><span class="line">	<span class="comment">//先跳到同一层</span></span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">if</span>(dep[up[u][i]]&gt;=dep[v])</span><br><span class="line">		&#123;</span><br><span class="line">			u=up[u][i];</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span>(u==v) <span class="keyword">return</span> v;</span><br><span class="line">	<span class="comment">//然后跳到LCA的下一层</span></span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">if</span>(up[u][i]!=up[v][i])</span><br><span class="line">		&#123;</span><br><span class="line">			u=up[u][i],v=up[v][i];</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> up[u][<span class="number">0</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="2-Tarijan">2.Tarijan</h3>
<p>时间复杂度：$O(V+E)$
特点：极其高效，但必须把所有询问一次性读入，是一种<strong>离线</strong>算法
<strong>核心思想</strong>：
1.<strong>DFS 遍历</strong>：从根节点开始深度优先搜索。
2.vi数组标记：
- <strong>未访问</strong>：还没搜到的节点。vi[u]=0
- <strong>正在访问</strong>：已经在递归栈中，但还没回溯完成的节点。vi[u]=1
- <strong>已访问</strong>：已经回溯完成，确定祖先关系的节点。 vi[u]=2
3.<strong>并查集维护</strong>：当一个节点 u回溯时，把它合并到它父节点的集合里。这意味着：对于已访问的节点，它的并查集根就是它在 DFS 树上的祖先。
<strong>关键逻辑</strong>：
当 DFS 到节点 u时，检查所有关于 u的询问 (u,v)：</p>
<ul>
<li>如果 v<strong>还没访问</strong>：不管。</li>
<li>如果 v<strong>已经访问过</strong>：那么 LCA(u,v)就是 <strong>v所在的并查集的根</strong>。</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">5e5</span><span class="number">+5</span>, M=<span class="number">1e5</span><span class="number">+5</span>;</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; adj[N];</span><br><span class="line"><span class="type">int</span> n,m;</span><br><span class="line">vector&lt;pair&lt;<span class="type">int</span>,<span class="type">int</span>&gt; &gt; query[N];</span><br><span class="line"><span class="type">int</span> ans[M], fa[N];</span><br><span class="line"><span class="type">int</span> vis[N]; <span class="comment">// 0=未访问, 1=正在访问, 2=已访问完毕</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(fa[x]==x) <span class="keyword">return</span> x;</span><br><span class="line">	<span class="keyword">return</span> fa[x]=<span class="built_in">find</span>(fa[x]);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">tarjan</span><span class="params">(<span class="type">int</span> u)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="comment">//入u时,标记u为正在访问</span></span><br><span class="line">	vis[u]=<span class="number">1</span>;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">auto</span> v:adj[u])</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">if</span>(!vis[v]) </span><br><span class="line">		&#123;</span><br><span class="line">			<span class="built_in">tarjan</span>(v);</span><br><span class="line">			<span class="comment">//回u时,v指向u</span></span><br><span class="line">			f[v]=u;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//离u时,枚举LCA</span></span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">auto</span> q:query[u])</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> v=q.first, i=q.second;</span><br><span class="line">		<span class="comment">// 必须是已经完全访问完毕(状态为2)的节点才能结算</span></span><br><span class="line">		<span class="keyword">if</span>(vis[v] == <span class="number">2</span>) ans[i]=<span class="built_in">find</span>(v);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 离开u时，标记u为已访问完毕</span></span><br><span class="line">	vis[u]=<span class="number">2</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	ios::<span class="built_in">sync_with_stdio</span>(<span class="literal">false</span>);</span><br><span class="line">	cin.<span class="built_in">tie</span>(<span class="literal">nullptr</span>);</span><br><span class="line">	cin&gt;&gt;n&gt;&gt;m;</span><br><span class="line">	<span class="type">int</span> a,b;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;n;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		cin&gt;&gt;a&gt;&gt;b;</span><br><span class="line">		adj[a].<span class="built_in">push_back</span>(b);</span><br><span class="line">		adj[b].<span class="built_in">push_back</span>(a);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=m;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		cin&gt;&gt;a&gt;&gt;b;</span><br><span class="line">		query[a].<span class="built_in">push_back</span>(&#123;b,i&#125;);</span><br><span class="line">		query[b].<span class="built_in">push_back</span>(&#123;a,i&#125;);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i) f[i]=i;</span><br><span class="line">	</span><br><span class="line">	<span class="built_in">tarjan</span>(<span class="number">1</span>); <span class="comment">// 假设 1 为根节点</span></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=m;++i) cout&lt;&lt;ans[i]&lt;&lt;<span class="string">&quot;\n&quot;</span>; </span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="3-树链剖分">3.树链剖分</h3>
<p>时间复杂度：$O(V + Q\log V)$
<strong>核心思想</strong>：<strong>将一棵树拆分成若干条互不相交的“链”</strong>。这样一来，树上的路径问题就被转化为了区间问题。
核心概念：</p>
<ul>
<li><strong>重儿子</strong>：某个节点的子节点中，<strong>子树节点最多</strong>的那个子节点。</li>
<li><strong>轻儿子</strong>：除重儿子以外的其他子节点。</li>
<li><strong>重边</strong>：连接父节点与重儿子的边。</li>
<li><strong>轻边</strong>：连接父节点与轻儿子的边。</li>
<li><strong>重链</strong>：由多条重边首尾相连组成的链。每个节点都属于且仅属于一条重链（单独的叶子节点或轻儿子自己也可以看作一条长度为 0 的重链）。</li>
<li><strong>链头</strong>：重链上深度最小的那个节点。
为了实现，我们需要两次dfs
第一次 DFS：收集树的基础信息
计算每个节点的深度（<code>dep</code>）、父亲（<code>fa</code>）、子树大小（<code>siz</code>），并找出每个节点的<strong>重儿子</strong>（<code>son</code>）。
第二次 DFS：连线成链
确定每个节点所在重链的<strong>链头</strong>（<code>top</code>）。优先走重儿子，再走轻儿子。
<strong>原理</strong>：</li>
<li>比较 <code>u</code> 和 <code>v</code> 所在的重链链头（<code>top[u]</code> 和 <code>top[v]</code>），<strong>谁的链头深度更深，谁就向上跳</strong>。</li>
<li>假设 <code>top[u]</code> 更深，我们就让 <code>u</code> 直接跳到它当前链头的父亲节点：<code>u = fa[top[u]]</code>。</li>
<li>重复这个过程，直到 <code>u</code> 和 <code>v</code> <strong>进入同一条重链</strong>（即 <code>top[u] == top[v]</code>）。</li>
<li>此时，<code>u</code> 和 <code>v</code> 已经在一条直链上了，<strong>深度较浅的那个节点就是它们的 LCA</strong>。</li>
</ul>
<pre><code class="language-cpp">// 核心数组
vector&lt;int&gt; adj[N];
int sz[N],fa[N],dep[N],son[N];
int top[N],id[N],cnt;

// 1. 求重儿子、size、深度、父节点
void dfs1(int u,int p){
    fa[u]=p; dep[u]=dep[p]+1; sz[u]=1;son[u]=0;
    for(int v:adj[u]){
        if(v==f)continue;
        dfs1(v,u);
        sz[u]+=sz[v];
        if(sz[v]&gt;sz[son[v]]) son[u]=v;
    }
}

// 2. 剖分链，赋id
void dfs2(int u,int t){
    top[u]=t; id[u]=++cnt;
    if(son[u]) dfs2(son[u],t); // 重儿子同链
    for(int v:adj[u]){
        if(v!=fa[u]&amp;&amp;v!=son[u])
            dfs2(v,v); // 轻儿子新开链
    }
}
int get_lca(int u,int v){
    while(top[u]!=top[v]){
        if(dep[top[u]]&lt;dep[top[v]]) swap(u,v);
        u=fa[top[u]];
    }
    if(dep[u]&gt;dep[v]) swap(u,v);
}
/*调用
dfs1(root, 0);
dfs2(root, root);
*/

</code></pre>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>算法</tag>
        <tag>C++</tag>
        <tag>图</tag>
      </tags>
  </entry>
  <entry>
    <title>ABC458</title>
    <url>/2026/05/16/ABC458/</url>
    <content><![CDATA[<p>今天写到E题，来写个小小题解吧！</p>
<h1 id="A">A</h1>
<h3 id="题意">题意</h3>
<p>给你一个字符串s，删除前面n个和后面n个字母，然后输出</p>
<h3 id="思路">思路</h3>
<p>使用substr进行截取</p>
<h3 id="代码">代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string s;</span><br><span class="line">    cin&gt;&gt;s;</span><br><span class="line">    <span class="type">int</span> n;</span><br><span class="line">    cin&gt;&gt;n;</span><br><span class="line">    s=s.<span class="built_in">substr</span>(n);</span><br><span class="line">    <span class="type">int</span> len=s.<span class="built_in">size</span>();</span><br><span class="line">    s=s.<span class="built_in">substr</span>(<span class="number">0</span>,len-n);</span><br><span class="line">    cout&lt;&lt;s&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="B">B</h1>
<h3 id="题意-v2">题意</h3>
<p>给你一个二维数组，问每一个单元格的相邻的单元格数</p>
<h3 id="思路-v2">思路</h3>
<p>超出范围的不算，我们可以假设都为4，然后如果有在边上的就剪掉</p>
<h3 id="代码-v2">代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   <span class="type">int</span> n,m;</span><br><span class="line">   cin&gt;&gt;n&gt;&gt;m;</span><br><span class="line">   <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">   &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=m;++j)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> f=<span class="number">4</span>;</span><br><span class="line">            <span class="keyword">if</span>(i==<span class="number">1</span>) f--;</span><br><span class="line">            <span class="keyword">if</span>(i==n) f--;</span><br><span class="line">            <span class="keyword">if</span>(j==<span class="number">1</span>) f--;</span><br><span class="line">            <span class="keyword">if</span>(j==m) f--;</span><br><span class="line">            cout&lt;&lt;f&lt;&lt;<span class="string">&quot; &quot;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        cout&lt;&lt;<span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="C">C</h1>
<h3 id="题意-v3">题意</h3>
<p>给你一个字符串s，寻找奇数并且&quot;C&quot;在中间的子串个数（不同位置的子串不同）</p>
<h3 id="思路-v3">思路</h3>
<p>只要找到每一个位置的”C“，并且计算在每个位置的最长的子串长度的一半，全部加起来</p>
<h3 id="代码-v3">代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string s;</span><br><span class="line">    cin&gt;&gt;s;</span><br><span class="line">    <span class="type">int</span> len=s.<span class="built_in">size</span>();</span><br><span class="line">    ll cnt=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;len;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(s[i]==<span class="string">&#x27;C&#x27;</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            cnt++;<span class="comment">//单独的&quot;C&quot;也是一个子串</span></span><br><span class="line">            cnt+=<span class="built_in">min</span>(i,len-i<span class="number">-1</span>);<span class="comment">//最长的长度取决于比较短的一边</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;cnt&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="D">D</h1>
<h3 id="题意-v4">题意</h3>
<p>给你一个x，然后进行q次加入ai和bi构成序列，并且查询序列的中位数</p>
<h3 id="思路-v4">思路</h3>
<p>刚开始以为是中间数，然后觉得怎么这么简单好奇怪，然后看了看样例
噢!中位数啊，要排序的！！！
然后想着排序？？于是想到优先队列（堆），但是呢这个是取中间的，不是求最大和最小啊
那我用两个！一大一小。
l是大根堆 用来存储所有数字中<strong>较小的那一半</strong>。 <code>l.top()</code>较小那一半之中的<strong>最大值</strong>。
r是小根堆（通过使用存入负值）用来存储所有数字中<strong>较大的那一半</strong>。<code>r.top()</code> 取负数后代表较大那一半之中的<strong>最小值</strong>。
因为每次循环都会同时插入两个数。加上初始的 1 个数，总数字个数序列永远是奇数。所以中位数是<code>l.top()</code></p>
<h3 id="代码-v4">代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">priority_queue&lt;<span class="type">int</span>&gt; l,r;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">insert</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(l.<span class="built_in">empty</span>()||x&lt;=l.<span class="built_in">top</span>())</span><br><span class="line">    &#123;</span><br><span class="line">	    <span class="comment">// 比左边最大值还小，说明属于较小的一半</span></span><br><span class="line">        l.<span class="built_in">push</span>(x);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">	    <span class="comment">// 比左边最大值大，说明属于较大的一半（存负数变小根堆）</span></span><br><span class="line">        r.<span class="built_in">push</span>(-x);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//调整两个堆的大小，左堆大小=右堆大小+1(保证l.top()是答案)</span></span><br><span class="line">    <span class="keyword">if</span>(l.<span class="built_in">size</span>()&gt;r.<span class="built_in">size</span>()<span class="number">+1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        r.<span class="built_in">push</span>(-l.<span class="built_in">top</span>());</span><br><span class="line">        l.<span class="built_in">pop</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(r.<span class="built_in">size</span>()&gt;l.<span class="built_in">size</span>())</span><br><span class="line">    &#123;</span><br><span class="line">        l.<span class="built_in">push</span>(-r.<span class="built_in">top</span>());</span><br><span class="line">        r.<span class="built_in">pop</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> x;</span><br><span class="line">    cin&gt;&gt;x;</span><br><span class="line">    <span class="built_in">insert</span>(x);</span><br><span class="line">    <span class="type">int</span> q;</span><br><span class="line">    cin&gt;&gt;q;</span><br><span class="line">    <span class="keyword">while</span>(q--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> a,b;</span><br><span class="line">        cin&gt;&gt;a&gt;&gt;b;</span><br><span class="line">        <span class="built_in">insert</span>(a);</span><br><span class="line">        <span class="built_in">insert</span>(b);</span><br><span class="line">        cout&lt;&lt;l.<span class="built_in">top</span>()&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="E">E</h1>
<h3 id="题意-v5">题意</h3>
<p>序列A满足有x1个1，x2个2，x3个3，并且 ∣ai+1​−ai​∣≤1 
问这样的A的个数，答案模998244353</p>
<h3 id="思路-v5">思路</h3>
<p>第一反应动态规划，正准备想转移状态，看了一下x1,x2,x3的范围，1~1e6,好吧，动态规划pass~~
重新开始看题目条件， ∣ai+1​−ai​∣≤1 ，那么我们推出 1和 3 绝对不能相邻，中间必须有2来隔开
那么！我们可以将2看成隔板，将1和3插入其中
将x2个 2 划分出的 x2+1 个空隙，<strong>每一个空隙要么全放 1（且至少放一个），要么全放 3（且至少放一个），要么都不放</strong>。
假设一共选了 i 个空隙来放 1，选了 j 个空隙来放 3。</p>
<ol>
<li><strong>选择空隙的位置</strong>：
从 x2+1个空隙中选出 i 个放 $1$，再从剩下的空隙中选出j个放 $3$,方案数为：
$\binom{x2+1}{i} \cdot \binom{x2+1-i}{j}$</li>
<li><strong>分配元素的数量</strong>：</li>
</ol>
<ul>
<li>将 $x1$ 个相同的 $1$ 放入 $i$ 个<strong>非空</strong>的空隙中，方案数为：$\binom{x1-1}{i-1}$</li>
<li>将 $x3$ 个相同的 $3$ 放入 $j$ 个<strong>非空</strong>的空隙中，方案数为：$\binom{x3-1}{j-1}$
所以，对于i和 j，合法的排列数为：</li>
</ul>
<p>$$f(i, j) = \binom{x2+1}{i} \binom{x2+1-i}{j} \binom{x1-1}{i-1} \binom{x3-1}{j-1}$$</p>
<p>然后我们对每一个i和j进行计算，但是这样时间就是1e12，oh no!TLE!
我们要在进行优化
当 $i$ 固定时，我们需要对 $j$ 进行求和：</p>
<p>$$\sum_{j=1}^{x3} \binom{x2+1}{i} \binom{x2+1-i}{j} \binom{x1-1}{i-1} \binom{x3-1}{j-1}$$</p>
<p>将与 $j$ 无关的项提出来：</p>
<p>$$\binom{x2+1}{i} \binom{x1-1}{i-1} \sum_{j=1}^{x3} \binom{x2+1-i}{j} \binom{x3-1}{j-1}$$</p>
<p>利用恒等式 $\binom{x3-1}{j-1} = \binom{x3-1}{x3-j}$，后面的求和项变为：</p>
<p>$$\sum_{j=1}^{x3} \binom{x2+1-i}{j} \binom{x3-1}{x3-j}$$</p>
<p>根据范德蒙德恒等式，这个求和等于从两堆物品（一堆 x2+1-i 个，另一堆 x3-1个）中一共选出 j + (x3-j) = x3 个物品的方案数
即：</p>
<p>$$\sum_{j=1}^{x3} \binom{x2+1-i}{j} \binom{x3-1}{x3-j} = \binom{x2+x3-i}{x3}$$</p>
<p>然后我们惊奇的发现 j 没掉了！
注意：i的个数不能大于x1,也不能大于可用的空隙x2+1
那么我们可以推出总方案数可以表示为：
$$ans = \sum_{i=1}^{\min(x1, x2+1)} \binom{x2+1}{i} \binom{x1-1}{i-1} \binom{x2+x3-i}{x3} \pmod{998244353}$$
代码上面
我们需要求组合数C，我们对阶乘 fact[]和逆元阶乘 invfact[]进行预处理
运用费马小定理和快速幂来求出最后一个invfact[N-1],然后倒推剩下的invfact[]</p>
<h3 id="代码-v5">代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">3e6</span><span class="number">+5</span>,mod=<span class="number">998244353</span>;</span><br><span class="line">ll fact[N],invfact[N];</span><br><span class="line"><span class="comment">//快速幂</span></span><br><span class="line"><span class="function">ll <span class="title">pw</span><span class="params">(ll a,ll b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ll res=<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(b)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(b&amp;<span class="number">1</span>)</span><br><span class="line">            res=(res*a)%mod;</span><br><span class="line">        a=(a*a)%mod;</span><br><span class="line">        b&gt;&gt;=<span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">pre</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    fact[<span class="number">0</span>]=<span class="number">1</span>;</span><br><span class="line">    invfact[<span class="number">0</span>]=<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;N;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        fact[i]=(fact[i<span class="number">-1</span>]*i)%mod;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//费马小定理</span></span><br><span class="line">    invfact[N<span class="number">-1</span>]=<span class="built_in">pw</span>(fact[N<span class="number">-1</span>],mod<span class="number">-2</span>);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=N<span class="number">-2</span>;i&gt;=<span class="number">1</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        invfact[i]=(invfact[i<span class="number">+1</span>]*(i<span class="number">+1</span>))%mod;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function">ll <span class="title">C</span><span class="params">(<span class="type">int</span> n,<span class="type">int</span> k)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(k&lt;<span class="number">0</span>||k&gt;n) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span> fact[n]*invfact[k]%mod*invfact[n-k]%mod;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">pre</span>();</span><br><span class="line">    <span class="type">int</span> x1,x2,x3;</span><br><span class="line">    cin&gt;&gt;x1&gt;&gt;x2&gt;&gt;x3;</span><br><span class="line">    ll ans=<span class="number">0</span>;</span><br><span class="line">    <span class="comment">//i的个数不能大于x1,也不能大于可用的空隙x2+1</span></span><br><span class="line">    <span class="type">int</span> n=<span class="built_in">min</span>(x1,x2<span class="number">+1</span>);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        ll sum1=<span class="built_in">C</span>(x2<span class="number">+1</span>,i)%mod;</span><br><span class="line">        ll sum2=<span class="built_in">C</span>(x1<span class="number">-1</span>,i<span class="number">-1</span>)%mod;</span><br><span class="line">        ll sum3=<span class="built_in">C</span>(x3+x2-i,x3)%mod;</span><br><span class="line">        ll now=((sum1*sum2)%mod*sum3)%mod;</span><br><span class="line">        ans=(ans+now)%mod;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;ans&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>算法</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>洛谷P4180 严格次小生成树</title>
    <url>/2026/05/24/%E6%B4%9B%E8%B0%B7P4180/</url>
    <content><![CDATA[<h1 id="题意">题意</h1>
<p>要找一棵“<strong>严格大于最小生成树、且权值和最小</strong>”的生成树</p>
<h1 id="思路">思路</h1>
<p>核心策略是“非树边替换树边”：</p>
<ol>
<li>先用 Kruskal 算法求出最小生成树，记录其权值总和为 <code>sum</code>。</li>
<li>遍历每一条<strong>不在最小生成树中</strong>的非树边 $e(u, v, w)$。如果把这条边强行加到树中，树上就会形成一个环（即 $u$ 到 $v$ 的树上路径加上这条新边）。</li>
<li>为了重新变成一棵树，我们需要在这个环里<strong>删掉一条原本就在树上的边</strong>。</li>
<li>为了让新的树权值总和增加得最少（且严格大于 <code>sum</code>），我们需要在 $u$ 到 $v$ 的树上路径中，找到权值最大、且<strong>严格小于 $w$</strong> 的那条树边替换掉。</li>
</ol>
<p>我使用了Kruskal来求最小生成树，倍增lca来查找 $u$ 到 $v$ 的树上路径中的替换边</p>
<h1 id="分块代码">分块代码</h1>
<p>看完思路后，我们一步一步来分块拆解代码</p>
<h2 id="1-Kruskal-求最小生成树">1.Kruskal 求最小生成树</h2>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">    <span class="type">int</span> u,v,w;</span><br><span class="line">    <span class="type">bool</span> vis;<span class="comment">//记录是不是树边</span></span><br><span class="line">    <span class="type">bool</span> <span class="keyword">operator</span>&lt;(<span class="type">const</span> Edge&amp; other) <span class="type">const</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> w &lt; other.w;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">kru</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;    </span><br><span class="line">    <span class="type">int</span> cnt=<span class="number">0</span>;</span><br><span class="line">    <span class="comment">//edg存储所有边，按照权值 w 从小到大排序</span></span><br><span class="line">    <span class="built_in">sort</span>(edg,edg+m);</span><br><span class="line">    <span class="comment">//并查集初始化</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i) fa[i]=i;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">auto</span>&amp; [u,v,w,vis]=edg[i];</span><br><span class="line">        <span class="type">int</span> ru=<span class="built_in">find</span>(u),rv=<span class="built_in">find</span>(v);</span><br><span class="line">        <span class="keyword">if</span>(ru!=rv)</span><br><span class="line">        &#123;</span><br><span class="line">            fa[ru]=rv;</span><br><span class="line">            cnt++;</span><br><span class="line">            sum+=w;</span><br><span class="line">            <span class="comment">// 标记这条边是树边</span></span><br><span class="line">            vis=<span class="number">1</span>;</span><br><span class="line">            <span class="comment">// 建立树的邻接表 adj</span></span><br><span class="line">            adj[v].<span class="built_in">push_back</span>(&#123;u,w&#125;);</span><br><span class="line">            adj[u].<span class="built_in">push_back</span>(&#123;v,w&#125;);</span><br><span class="line">            <span class="keyword">if</span>(cnt==n<span class="number">-1</span>) <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> vis=<span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="2-树上倍增预处理">2.树上倍增预处理</h2>
<p>为了高效查询树上 $u$ 到 $v$ 路径间的<strong>最大边权</strong>和<strong>次大边权</strong>，使用 LCA（最近公共祖先）的<strong>倍增算法</strong></p>
<ul>
<li><code>up[u][i]</code>：表示节点 <code>u</code> 向上跳 $2^i$ 步到达的祖先。</li>
<li><code>w1[u][i]</code>：表示从 <code>u</code> 向上到 <code>up[u][i]</code> 的路径上的<strong>最大边权</strong>。</li>
<li><code>w2[u][i]</code>：表示该路径上的<strong>次大边权</strong></li>
</ul>
<p>其中dfs中它们被动态维护</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> p,<span class="type">int</span> ww)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    dep[u]=dep[p]<span class="number">+1</span>;</span><br><span class="line">    up[u][<span class="number">0</span>]=p;</span><br><span class="line">    w1[u][<span class="number">0</span>]=ww,w2[u][<span class="number">0</span>]=-inf;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=<span class="number">19</span>;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        up[u][i]=up[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>];</span><br><span class="line">        <span class="comment">// 路径最大值，显然是两段各自最大值中的较大者</span></span><br><span class="line">        w1[u][i]=<span class="built_in">max</span>(w1[u][i<span class="number">-1</span>],w1[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>]);</span><br><span class="line">        <span class="comment">//路径次大值，情况稍微复杂一点</span></span><br><span class="line">        <span class="comment">// 先从两段的次大值里挑个大的</span></span><br><span class="line">        w2[u][i]=<span class="built_in">max</span>(w2[u][i<span class="number">-1</span>],w2[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>]);</span><br><span class="line">        <span class="comment">// 如果两段的最大值不一样，那么其中较小的那个最大值，是整段的“次大值”</span></span><br><span class="line">        <span class="keyword">if</span>(w1[u][i<span class="number">-1</span>]!=w1[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>])</span><br><span class="line">        &#123;</span><br><span class="line">            w2[u][i]=<span class="built_in">max</span>(w2[u][i],<span class="built_in">min</span>(w1[u][i<span class="number">-1</span>],w1[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>]));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> [vv,ww]:adj[u])</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(vv==p) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="built_in">dfs</span>(vv,u,ww);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="3-查询与替换函数">3.查询与替换函数</h2>
<p>当要尝试加入一条非树边 $(u, v, w)$ 时，我们要去捞出 $u$ 到 $v$ 路径上符合要求的最大替换边
也就是$u-&gt;l$ 和$l-&gt;v$这个路径上面的最大替换边</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">F</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> v,<span class="type">int</span> w)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> res=-inf;</span><br><span class="line">    <span class="type">int</span> l=<span class="built_in">lca</span>(u,v);</span><br><span class="line">    <span class="comment">//u-&gt;l</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">	    <span class="comment">//向上倍增跳跃</span></span><br><span class="line">        <span class="keyword">if</span>(up[u][i]&amp;&amp;dep[up[u][i]]&gt;=dep[l])</span><br><span class="line">	    &#123;</span><br><span class="line">		    <span class="comment">// 如果最大树边小于 w，直接尝试用它</span></span><br><span class="line">            <span class="keyword">if</span>(w1[u][i]&lt;w)</span><br><span class="line">            &#123;</span><br><span class="line">                res=<span class="built_in">max</span>(res,w1[u][i]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[u][i]);<span class="comment">// 如果最大树边等于 w，只能退而求其次用次大树边</span></span><br><span class="line">            u=up[u][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//l-&gt;v就是v-&gt;l</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(up[v][i]&amp;&amp;dep[up[v][i]]&gt;=dep[l])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(w1[v][i]&lt;w)</span><br><span class="line">            &#123;</span><br><span class="line">                res=<span class="built_in">max</span>(res,w1[v][i]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[v][i]);</span><br><span class="line">            v=up[v][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="4-最后枚举非树边寻找答案">4.最后枚举非树边寻找答案</h2>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">ll ans=<span class="number">1e18</span>;</span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">auto</span> [u,v,w,vis]=edg[i];</span><br><span class="line">    <span class="keyword">if</span>(!vis&amp;&amp;u!=v)</span><br><span class="line">    &#123;</span><br><span class="line">	    <span class="comment">//新树的总权值=原来的sum-被删掉的树边val+新加入的非树边w</span></span><br><span class="line">        <span class="type">int</span> val=<span class="built_in">F</span>(u,v,w);</span><br><span class="line">        <span class="keyword">if</span>(val&gt;-inf)</span><br><span class="line">        &#123;</span><br><span class="line">            ans=<span class="built_in">min</span>(ans,sum-val+w);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">cout&lt;&lt;ans&lt;&lt;<span class="string">&#x27;\n&#x27;</span>;</span><br></pre></td></tr></table></figure>
<h1 id="最终代码">最终代码</h1>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">1e5</span><span class="number">+5</span>,M=<span class="number">3e5</span><span class="number">+5</span>,inf=<span class="number">2e9</span>;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">    <span class="type">int</span> u,v,w;</span><br><span class="line">    <span class="type">bool</span> vis;</span><br><span class="line">    <span class="type">bool</span> <span class="keyword">operator</span>&lt;(<span class="type">const</span> Edge&amp; other) <span class="type">const</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> w &lt; other.w;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge1</span>&#123;</span><br><span class="line">    <span class="type">int</span> v,w;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="type">int</span> fa[N];</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(fa[x]==x) <span class="keyword">return</span> x;</span><br><span class="line">    <span class="keyword">return</span> fa[x]=<span class="built_in">find</span>(fa[x]);</span><br><span class="line">&#125;</span><br><span class="line">vector&lt;Edge1&gt; adj[N];</span><br><span class="line"><span class="type">int</span> up[N][<span class="number">20</span>],dep[N],w1[N][<span class="number">20</span>],w2[N][<span class="number">20</span>];</span><br><span class="line">Edge edg[M];</span><br><span class="line">ll sum=<span class="number">0</span>;</span><br><span class="line"><span class="type">int</span> n,m;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">kru</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;    </span><br><span class="line">    <span class="type">int</span> cnt=<span class="number">0</span>;</span><br><span class="line">    <span class="built_in">sort</span>(edg,edg+m);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i) fa[i]=i;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">auto</span>&amp; [u,v,w,vis]=edg[i];</span><br><span class="line">        <span class="type">int</span> ru=<span class="built_in">find</span>(u),rv=<span class="built_in">find</span>(v);</span><br><span class="line">        <span class="keyword">if</span>(ru!=rv)</span><br><span class="line">        &#123;</span><br><span class="line">            fa[ru]=rv;</span><br><span class="line">            cnt++;</span><br><span class="line">            sum+=w;</span><br><span class="line">            vis=<span class="number">1</span>;</span><br><span class="line">            adj[v].<span class="built_in">push_back</span>(&#123;u,w&#125;);</span><br><span class="line">            adj[u].<span class="built_in">push_back</span>(&#123;v,w&#125;);</span><br><span class="line">            <span class="keyword">if</span>(cnt==n<span class="number">-1</span>) <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> vis=<span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> p,<span class="type">int</span> ww)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    dep[u]=dep[p]<span class="number">+1</span>;</span><br><span class="line">    up[u][<span class="number">0</span>]=p;</span><br><span class="line">    w1[u][<span class="number">0</span>]=ww,w2[u][<span class="number">0</span>]=-inf;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=<span class="number">19</span>;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        up[u][i]=up[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>];</span><br><span class="line">        w1[u][i]=<span class="built_in">max</span>(w1[u][i<span class="number">-1</span>],w1[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>]);</span><br><span class="line">        w2[u][i]=<span class="built_in">max</span>(w2[u][i<span class="number">-1</span>],w2[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>]);</span><br><span class="line">        <span class="keyword">if</span>(w1[u][i<span class="number">-1</span>]!=w1[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>])</span><br><span class="line">        &#123;</span><br><span class="line">            w2[u][i]=<span class="built_in">max</span>(w2[u][i],<span class="built_in">min</span>(w1[u][i<span class="number">-1</span>],w1[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>]));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> [vv,ww]:adj[u])</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(vv==p) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="built_in">dfs</span>(vv,u,ww);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">lca</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> v)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(dep[v]&gt;dep[u]) <span class="built_in">swap</span>(u,v);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(dep[up[u][i]]&gt;=dep[v])</span><br><span class="line">        &#123;</span><br><span class="line">            u=up[u][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(u==v)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> u;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(up[u][i]!=up[v][i])</span><br><span class="line">        &#123;</span><br><span class="line">            u=up[u][i];</span><br><span class="line">            v=up[v][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> up[u][<span class="number">0</span>];</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">F</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> v,<span class="type">int</span> w)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> res=-inf;</span><br><span class="line">    <span class="type">int</span> l=<span class="built_in">lca</span>(u,v);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(up[u][i]&amp;&amp;dep[up[u][i]]&gt;=dep[l])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(w1[u][i]&lt;w)</span><br><span class="line">            &#123;</span><br><span class="line">                res=<span class="built_in">max</span>(res,w1[u][i]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[u][i]);</span><br><span class="line">            u=up[u][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(up[v][i]&amp;&amp;dep[up[v][i]]&gt;=dep[l])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(w1[v][i]&lt;w)</span><br><span class="line">            &#123;</span><br><span class="line">                res=<span class="built_in">max</span>(res,w1[v][i]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[v][i]);</span><br><span class="line">            v=up[v][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cin&gt;&gt;n&gt;&gt;m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> x,y,z;</span><br><span class="line">        cin&gt;&gt;x&gt;&gt;y&gt;&gt;z;</span><br><span class="line">        edg[i]=&#123;x,y,z&#125;;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">kru</span>();</span><br><span class="line">    <span class="built_in">dfs</span>(<span class="number">1</span>,<span class="number">0</span>,-inf);</span><br><span class="line">    ll ans=<span class="number">1e18</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">auto</span> [u,v,w,vis]=edg[i];</span><br><span class="line">        <span class="keyword">if</span>(!vis&amp;&amp;u!=v)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> val=<span class="built_in">F</span>(u,v,w);</span><br><span class="line">            <span class="keyword">if</span>(val&gt;-inf)</span><br><span class="line">            &#123;</span><br><span class="line">                ans=<span class="built_in">min</span>(ans,sum-val+w);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;ans&lt;&lt;<span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ios::<span class="built_in">sync_with_stdio</span>(<span class="literal">false</span>);</span><br><span class="line">    cin.<span class="built_in">tie</span>(<span class="literal">nullptr</span>);</span><br><span class="line">    <span class="type">int</span> T=<span class="number">1</span>;</span><br><span class="line">    <span class="comment">//cin&gt;&gt;T;</span></span><br><span class="line">    <span class="keyword">while</span>(T--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">solve</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="后记">后记</h1>
<p>有人问既然是在$u$到$v$的路径，那么能不能<strong>直接</strong>在倍增lca的过程中直接求res呢
当然可以！！</p>
<h3 id="LCA代码">LCA代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">lca</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> v,<span class="type">int</span> w)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> res=-inf;</span><br><span class="line">    <span class="keyword">if</span>(dep[v]&gt;dep[u]) <span class="built_in">swap</span>(u,v);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(dep[up[u][i]]&gt;=dep[v])</span><br><span class="line">        &#123;</span><br><span class="line">	        <span class="comment">// 如果最大树边小于 w，直接尝试用它</span></span><br><span class="line">            <span class="keyword">if</span>(w1[u][i]&lt;w) res=<span class="built_in">max</span>(res,w1[u][i]);</span><br><span class="line">            <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[u][i]);<span class="comment">// 如果最大树边等于 w，只能退而求其次用次大树边</span></span><br><span class="line">            u=up[u][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(u==v)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(up[u][i]!=up[v][i])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(w1[u][i]&lt;w)</span><br><span class="line">                res=<span class="built_in">max</span>(res,w1[u][i]);</span><br><span class="line">            <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[u][i]);</span><br><span class="line">            <span class="keyword">if</span>(w1[v][i]&lt;w)</span><br><span class="line">                res=<span class="built_in">max</span>(res,w1[v][i]);</span><br><span class="line">            <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[v][i]);</span><br><span class="line">            u=up[u][i];</span><br><span class="line">            v=up[v][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//最后需要判断一下up[u][0]和up[v][0]</span></span><br><span class="line">    <span class="keyword">if</span>(w1[u][<span class="number">0</span>]&lt;w)</span><br><span class="line">        res=<span class="built_in">max</span>(res,w1[u][<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[u][<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">if</span>(w1[v][<span class="number">0</span>]&lt;w)</span><br><span class="line">        res=<span class="built_in">max</span>(res,w1[v][<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[v][<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="总代码">总代码</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">1e5</span><span class="number">+5</span>,M=<span class="number">3e5</span><span class="number">+5</span>,inf=<span class="number">2e9</span>;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">    <span class="type">int</span> u,v,w;</span><br><span class="line">    <span class="type">bool</span> vis;</span><br><span class="line">    <span class="type">bool</span> <span class="keyword">operator</span>&lt;(<span class="type">const</span> Edge&amp; other) <span class="type">const</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> w &lt; other.w;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge1</span>&#123;</span><br><span class="line">    <span class="type">int</span> v,w;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="type">int</span> fa[N];</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(fa[x]==x) <span class="keyword">return</span> x;</span><br><span class="line">    <span class="keyword">return</span> fa[x]=<span class="built_in">find</span>(fa[x]);</span><br><span class="line">&#125;</span><br><span class="line">vector&lt;Edge1&gt; adj[N];</span><br><span class="line"><span class="type">int</span> up[N][<span class="number">20</span>],dep[N],w1[N][<span class="number">20</span>],w2[N][<span class="number">20</span>];</span><br><span class="line">Edge edg[M];</span><br><span class="line">ll sum=<span class="number">0</span>;</span><br><span class="line"><span class="type">int</span> n,m;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">kru</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;    </span><br><span class="line">    <span class="type">int</span> cnt=<span class="number">0</span>;</span><br><span class="line">    <span class="built_in">sort</span>(edg,edg+m);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i) fa[i]=i;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">auto</span>&amp; [u,v,w,vis]=edg[i];</span><br><span class="line">        <span class="type">int</span> ru=<span class="built_in">find</span>(u),rv=<span class="built_in">find</span>(v);</span><br><span class="line">        <span class="keyword">if</span>(ru!=rv)</span><br><span class="line">        &#123;</span><br><span class="line">            fa[ru]=rv;</span><br><span class="line">            cnt++;</span><br><span class="line">            sum+=w;</span><br><span class="line">            vis=<span class="number">1</span>;</span><br><span class="line">            adj[v].<span class="built_in">push_back</span>(&#123;u,w&#125;);</span><br><span class="line">            adj[u].<span class="built_in">push_back</span>(&#123;v,w&#125;);</span><br><span class="line">            <span class="keyword">if</span>(cnt==n<span class="number">-1</span>) <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> vis=<span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> p,<span class="type">int</span> ww)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    dep[u]=dep[p]<span class="number">+1</span>;</span><br><span class="line">    up[u][<span class="number">0</span>]=p;</span><br><span class="line">    w1[u][<span class="number">0</span>]=ww,w2[u][<span class="number">0</span>]=-inf;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=<span class="number">19</span>;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        up[u][i]=up[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>];</span><br><span class="line">        w1[u][i]=<span class="built_in">max</span>(w1[u][i<span class="number">-1</span>],w1[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>]);</span><br><span class="line">        w2[u][i]=<span class="built_in">max</span>(w2[u][i<span class="number">-1</span>],w2[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>]);</span><br><span class="line">        <span class="keyword">if</span>(w1[u][i<span class="number">-1</span>]!=w1[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>])</span><br><span class="line">        &#123;</span><br><span class="line">            w2[u][i]=<span class="built_in">max</span>(w2[u][i],<span class="built_in">min</span>(w1[u][i<span class="number">-1</span>],w1[up[u][i<span class="number">-1</span>]][i<span class="number">-1</span>]));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> [vv,ww]:adj[u])</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(vv==p) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="built_in">dfs</span>(vv,u,ww);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">lca</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> v,<span class="type">int</span> w)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> res=-inf;</span><br><span class="line">    <span class="keyword">if</span>(dep[v]&gt;dep[u]) <span class="built_in">swap</span>(u,v);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(dep[up[u][i]]&gt;=dep[v])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(w1[u][i]&lt;w) res=<span class="built_in">max</span>(res,w1[u][i]);</span><br><span class="line">            <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[u][i]);</span><br><span class="line">            u=up[u][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(u==v)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">19</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(up[u][i]!=up[v][i])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(w1[u][i]&lt;w)</span><br><span class="line">                res=<span class="built_in">max</span>(res,w1[u][i]);</span><br><span class="line">            <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[u][i]);</span><br><span class="line">            <span class="keyword">if</span>(w1[v][i]&lt;w)</span><br><span class="line">                res=<span class="built_in">max</span>(res,w1[v][i]);</span><br><span class="line">            <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[v][i]);</span><br><span class="line">            u=up[u][i];</span><br><span class="line">            v=up[v][i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(w1[u][<span class="number">0</span>]&lt;w)</span><br><span class="line">        res=<span class="built_in">max</span>(res,w1[u][<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[u][<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">if</span>(w1[v][<span class="number">0</span>]&lt;w)</span><br><span class="line">        res=<span class="built_in">max</span>(res,w1[v][<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">else</span> res=<span class="built_in">max</span>(res,w2[v][<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cin&gt;&gt;n&gt;&gt;m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> x,y,z;</span><br><span class="line">        cin&gt;&gt;x&gt;&gt;y&gt;&gt;z;</span><br><span class="line">        edg[i]=&#123;x,y,z&#125;;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">kru</span>();</span><br><span class="line">    <span class="built_in">dfs</span>(<span class="number">1</span>,<span class="number">0</span>,-inf);</span><br><span class="line">    ll ans=<span class="number">1e18</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">auto</span> [u,v,w,vis]=edg[i];</span><br><span class="line">        <span class="keyword">if</span>(!vis&amp;&amp;u!=v)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> val=<span class="built_in">lca</span>(u,v,w);</span><br><span class="line">            <span class="keyword">if</span>(val&gt;-inf)</span><br><span class="line">            &#123;</span><br><span class="line">                ans=<span class="built_in">min</span>(ans,sum-val+w);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;ans&lt;&lt;<span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ios::<span class="built_in">sync_with_stdio</span>(<span class="literal">false</span>);</span><br><span class="line">    cin.<span class="built_in">tie</span>(<span class="literal">nullptr</span>);</span><br><span class="line">    <span class="type">int</span> T=<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(T--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">solve</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>图</tag>
      </tags>
  </entry>
  <entry>
    <title>ABC459</title>
    <url>/2026/05/23/ABC459/</url>
    <content><![CDATA[<p>今天只写到D题，但是我还是来写题解了~
A和B感觉没什么好写的，略了。</p>
<h1 id="C">C</h1>
<h2 id="题意">题意</h2>
<p>给你N个格子，进行Q次操作
1.在第x个格子里面加1，如果每个格子都大于1，每个格子都需要减去1
2.查询有几个格子大于x</p>
<h2 id="思路">思路</h2>
<p>刚开始想着直接暴力，但是一看范围，嗯100%超时
每次所有格子大于1的时候都减掉1，如果暴力遍历所有格子好麻烦，那么！我们干脆不要减了，<strong>记录下需要减的次数cnt</strong>
在查询x的时候，因为我们没有给每个格子减cnt，所以我们需要给x<strong>加上tar</strong>是我们最后得出需要查询的值$tar=cnt+x$
解决了减掉1的麻烦，还有什么麻烦呢？有没有感觉查询大于tar如果每次都遍历一次好麻烦，那么！如何快速的找到几个格子大于tar呢？我们直接开一个<em><strong>数组<code>S[i]</code>来记录累计次数&gt;=i 的单元格数量</strong>，现在我们查询只要输出<code>S[tar]</code>即可
好了，问题又来了，我们如何看是否需要<code>cnt++</code>(所有格子减1)，其实思考完上面，这个解决非常简单，只有当</em><em>所有单元格的次数都大于cnt</em>*的时候，我们才要减掉1，我们已经记录了次数数组S，所以只要$S[cnt+1]==n$ 我们就需要<code>cnt++</code></p>
<h2 id="代码">代码</h2>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> n,q;</span><br><span class="line">    cin&gt;&gt;n&gt;&gt;q;</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">a</span><span class="params">(n<span class="number">+1</span>,<span class="number">0</span>)</span></span>;      </span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">S</span><span class="params">(q<span class="number">+5</span>,<span class="number">0</span>)</span></span>;    </span><br><span class="line">    S[<span class="number">0</span>]=n;</span><br><span class="line">    <span class="type">int</span> cnt=<span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> qq=q;</span><br><span class="line">    <span class="keyword">while</span>(q--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> op,x;</span><br><span class="line">        cin&gt;&gt;op&gt;&gt;x;</span><br><span class="line">        <span class="keyword">if</span> (op==<span class="number">1</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> c=a[x];</span><br><span class="line">            S[c<span class="number">+1</span>]++;</span><br><span class="line">            a[x]++;</span><br><span class="line">            <span class="keyword">if</span> (S[cnt<span class="number">+1</span>]==n)</span><br><span class="line">            &#123;</span><br><span class="line">                cnt++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (op==<span class="number">2</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> tar=cnt+x;</span><br><span class="line">            <span class="comment">// 如果目标要求的累计次数超过了最大操作数q，直接输出0</span></span><br><span class="line">            <span class="keyword">if</span>(tar&gt;qq)</span><br><span class="line">            &#123;</span><br><span class="line">                cout&lt;&lt;<span class="string">&quot;0\n&quot;</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                cout&lt;&lt;S[tar]&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="D">D</h1>
<h2 id="题意-v2">题意</h2>
<p>给你字符串S，问能不能重新排列 S ，让相邻的两个字符都不相同</p>
<h2 id="思路-v2">思路</h2>
<p>找到S中出现最多的字母的个数maxc，l是S的长度，如果最多的字母的个数大于长度的一半+1即$maxc&gt;(l+1)/2$，那么肯定不可以，直接输出No
如果可以，还需要输出排序后的S
那么现在我们需要构造S，其实只要交替的放字母就好了，这里我使用了优先队列（堆）来实现</p>
<h2 id="代码-v2">代码</h2>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> n;</span><br><span class="line">    cin&gt;&gt;n;</span><br><span class="line">    <span class="keyword">while</span>(n--)</span><br><span class="line">    &#123;</span><br><span class="line">        string s;</span><br><span class="line">        cin&gt;&gt;s;</span><br><span class="line">        map&lt;<span class="type">char</span>,<span class="type">int</span>&gt; mp;</span><br><span class="line">        <span class="type">int</span> l=s.<span class="built_in">size</span>();</span><br><span class="line">        <span class="type">int</span> maxc=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;l;++i)</span><br><span class="line">        &#123;</span><br><span class="line">            mp[s[i]]++;</span><br><span class="line">            <span class="keyword">if</span>(mp[s[i]]&gt;maxc)</span><br><span class="line">            &#123;</span><br><span class="line">                maxc=mp[s[i]];</span><br><span class="line">            &#125;  </span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(maxc&lt;=(l<span class="number">+1</span>)/<span class="number">2</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            cout&lt;&lt;<span class="string">&quot;Yes\n&quot;</span>;</span><br><span class="line">            priority_queue&lt;pair&lt;<span class="type">int</span>, <span class="type">char</span>&gt; &gt; pq;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> i=<span class="number">0</span>;i&lt;<span class="number">26</span>;i++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span>(mp[i+<span class="string">&#x27;a&#x27;</span>])</span><br><span class="line">                &#123;</span><br><span class="line">                    pq.<span class="built_in">push</span>(&#123;mp[i+<span class="string">&#x27;a&#x27;</span>],i+<span class="string">&#x27;a&#x27;</span>&#125;);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            string ans=<span class="string">&quot;&quot;</span>;</span><br><span class="line">            <span class="function">pair&lt;<span class="type">int</span>,<span class="type">char</span>&gt; <span class="title">pre</span><span class="params">(<span class="number">-1</span>,<span class="string">&#x27; &#x27;</span>)</span></span>;</span><br><span class="line">            <span class="keyword">while</span>(!pq.<span class="built_in">empty</span>())</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">auto</span> now=pq.<span class="built_in">top</span>();</span><br><span class="line">                pq.<span class="built_in">pop</span>();</span><br><span class="line">                ans+=now.second;</span><br><span class="line">                now.first--;</span><br><span class="line">                <span class="keyword">if</span>(pre.first&gt;<span class="number">0</span>)</span><br><span class="line">                &#123;</span><br><span class="line">                    pq.<span class="built_in">push</span>(pre);</span><br><span class="line">                &#125;</span><br><span class="line">                pre=now;</span><br><span class="line">            &#125;</span><br><span class="line">            cout&lt;&lt;ans&lt;&lt;<span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> cout&lt;&lt;<span class="string">&quot;No\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>算法</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>并查集题型</title>
    <url>/2026/07/26/%E5%B9%B6%E6%9F%A5%E9%9B%86%E9%A2%98%E5%9E%8B/</url>
    <content><![CDATA[<p>并查集解决的是这样一个标准问题：<strong>动态维护若干个不相交的集合，支持合并两个集合与查询两个元素是否属于同一集合</strong>。但在实际问题中，很少会直接给出“请合并集合”的题目。更多时候，我们需要先从题意中找到“连通关系”或“等价关系”，再判断题目究竟需要这种关系的什么信息。</p>
<p>因此，并查集题型可以沿着两条主线展开：</p>
<ul>
<li><strong>怎样识别并查集模型</strong>：对象是什么，什么算“连通”或“属于同一集合”；</li>
<li><strong>怎样询问</strong>：求连通块数量、集合大小、判断是否成环、带权关系推断，还是扩展域的分类问题。</li>
</ul>
<h2 id="一、怎样从原问题中找到并查集模型">一、怎样从原问题中找到并查集模型</h2>
<h3 id="1-显式的连通性问题">1. 显式的连通性问题</h3>
<p>问题直接描述“连通”、“相连”、“属于同一组”等概念，是最直接的并查集应用场景。</p>
<h4 id="典型例题：P1551-亲戚">典型例题：<a href="https://www.luogu.com.cn/problem/P1551">P1551 亲戚</a></h4>
<p>给定 n 个人、m 对亲戚关系，以及 p 个询问，每次询问两个人是否为亲戚。亲戚关系具有传递性。</p>
<p><strong>解法思路</strong>：每个人初始是一个独立的集合。每读入一对亲戚关系，就将两人所在的集合合并。查询时只需判断两人是否在同一集合中。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> maxn=<span class="number">5000</span><span class="number">+5</span>;</span><br><span class="line"><span class="type">int</span> f[maxn];</span><br><span class="line"><span class="type">int</span> n,m;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(f[x]==x) <span class="keyword">return</span> x;</span><br><span class="line">	<span class="keyword">return</span> f[x]=<span class="built_in">find</span>(f[x]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> q;</span><br><span class="line">    cin&gt;&gt;n&gt;&gt;m&gt;&gt;q;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i) f[i]=i;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> x,y;</span><br><span class="line">		cin&gt;&gt;x&gt;&gt;y;</span><br><span class="line">		<span class="type">int</span> fx=<span class="built_in">find</span>(x);</span><br><span class="line">		<span class="type">int</span> fy=<span class="built_in">find</span>(y);</span><br><span class="line">		<span class="keyword">if</span>(fx!=fy)</span><br><span class="line">		&#123;</span><br><span class="line">			f[fx]=fy;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;q;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> x,y;</span><br><span class="line">		cin&gt;&gt;x&gt;&gt;y;</span><br><span class="line">		<span class="keyword">if</span>(<span class="built_in">find</span>(x)==<span class="built_in">find</span>(y))cout&lt;&lt;<span class="string">&quot;Yes\n&quot;</span>;</span><br><span class="line">		<span class="keyword">else</span> cout&lt;&lt;<span class="string">&quot;No\n&quot;</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr>
<h3 id="2-关系具有传递性的问题">2. 关系具有传递性的问题</h3>
<p>当题目中的关系满足“A 与 B 有关系，B 与 C 有关系 ⇒ A 与 C 也有关系”时，就可以用并查集来维护这种等价关系。</p>
<h4 id="典型例题：P1955-程序自动分析">典型例题：<a href="https://www.luogu.com.cn/problem/P1955">P1955 程序自动分析</a></h4>
<p>给定若干对变量之间的相等/不等约束，判断是否存在一组赋值使所有约束同时满足。</p>
<p>数据规模较大，变量编号可达 10^9，需要<strong>离散化</strong>处理。</p>
<p><strong>解法思路</strong>：将所有相等约束涉及的变量合并到同一集合，然后检查所有不等约束——如果两个变量已在同一集合中，则矛盾。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> maxn=<span class="number">2e5</span><span class="number">+5</span>;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Disc</span>&#123;</span><br><span class="line">	vector&lt;<span class="type">int</span>&gt; v;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">add</span><span class="params">(<span class="type">int</span> x)</span> </span>&#123; v.<span class="built_in">push_back</span>(x); &#125;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">init</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">sort</span>(v.<span class="built_in">begin</span>(),v.<span class="built_in">end</span>());</span><br><span class="line">		v.<span class="built_in">erase</span>(<span class="built_in">unique</span>(v.<span class="built_in">begin</span>(),v.<span class="built_in">end</span>()),v.<span class="built_in">end</span>());</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="type">int</span> <span class="title">size</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> v.<span class="built_in">size</span>(); &#125;</span><br><span class="line">	<span class="function"><span class="type">int</span> <span class="title">get_idx</span><span class="params">(<span class="type">int</span> x)</span> </span>&#123; <span class="keyword">return</span> <span class="built_in">lower_bound</span>(v.<span class="built_in">begin</span>(),v.<span class="built_in">end</span>(),x)-v.<span class="built_in">begin</span>()<span class="number">+1</span>; &#125;</span><br><span class="line">	<span class="function"><span class="type">int</span> <span class="title">get_val</span><span class="params">(<span class="type">int</span> idx)</span> </span>&#123; <span class="keyword">return</span> v[idx<span class="number">-1</span>]; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="type">int</span> n;</span><br><span class="line"><span class="type">int</span> f[maxn];</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">node</span>&#123;</span><br><span class="line">	<span class="type">int</span> x,y,op;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">cmp</span><span class="params">(node a,node b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> a.op&gt;b.op;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(f[x]==x) <span class="keyword">return</span> x;</span><br><span class="line">	<span class="keyword">return</span> f[x]=<span class="built_in">find</span>(f[x]);</span><br><span class="line">&#125;</span><br><span class="line">node a[maxn];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	Disc disc;</span><br><span class="line">	cin&gt;&gt;n;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;n;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> x,y,z;</span><br><span class="line">		cin&gt;&gt;x&gt;&gt;y&gt;&gt;z;</span><br><span class="line">		a[i]=&#123;x,y,z&#125;;</span><br><span class="line">		disc.<span class="built_in">add</span>(x),disc.<span class="built_in">add</span>(y);</span><br><span class="line">	&#125;</span><br><span class="line">	disc.<span class="built_in">init</span>();</span><br><span class="line">	<span class="built_in">sort</span>(a,a+n,cmp);</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;N;++i) f[i]=i;</span><br><span class="line">	<span class="type">int</span> ff=<span class="number">1</span>;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;n;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">auto</span> [x,y,op]=a[i];</span><br><span class="line">		x=disc.<span class="built_in">get_idx</span>(x),y=disc.<span class="built_in">get_idx</span>(y);</span><br><span class="line">		<span class="type">int</span> fx=<span class="built_in">find</span>(x),fy=<span class="built_in">find</span>(y);</span><br><span class="line">		<span class="keyword">if</span>(op==<span class="number">1</span>)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">if</span>(fx!=fy) f[fx]=fy;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="keyword">if</span>(fx==fy) </span><br><span class="line">			&#123;</span><br><span class="line">				ff=<span class="number">0</span>;</span><br><span class="line">				<span class="keyword">break</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span>(ff) cout&lt;&lt;<span class="string">&quot;YES\n&quot;</span>;</span><br><span class="line">	<span class="keyword">else</span> cout&lt;&lt;<span class="string">&quot;NO\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这一类问题的核心是：<strong>关系的传递性 = 并查集的合并</strong>。</p>
<hr>
<h3 id="3-动态连通性问题（离线倒序）">3. 动态连通性问题（离线倒序）</h3>
<p>并查集擅长处理<strong>动态添加边</strong>的问题，但当涉及<strong>删除/断开</strong>操作时，直接处理很困难。常用技巧是<strong>离线倒序</strong>——把删除看作反向的添加。</p>
<h4 id="典型例题：P1197-JSOI2008-星球大战">典型例题：<a href="https://www.luogu.com.cn/problem/P1197">P1197 [JSOI2008] 星球大战</a></h4>
<p>给定一张无向图，依次摧毁一些节点及其连边，求每次摧毁后连通块的数量。</p>
<p><strong>解法思路</strong>：正着做是删边，并查集不支持这样的操作。因此<strong>离线读入所有操作，从最终状态开始倒序加边</strong>。先记录所有被摧毁的节点，建出最终剩下的图，然后<strong>倒序</strong>处理摧毁操作——每次“恢复”一个节点及其连边，用并查集合并。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> maxn=<span class="number">4e5</span><span class="number">+5</span>;</span><br><span class="line"><span class="type">int</span> n,m;</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; adj[maxn];</span><br><span class="line"><span class="type">int</span> fa[maxn],ans[maxn],a[maxn],vis[maxn];</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(fa[x]==x) <span class="keyword">return</span> x;</span><br><span class="line">    <span class="keyword">return</span> fa[x]=<span class="built_in">find</span>(fa[x]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	cin&gt;&gt;n&gt;&gt;m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;n;++i) fa[i]=i;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> u,v;</span><br><span class="line">        cin&gt;&gt;u&gt;&gt;v;</span><br><span class="line">        adj[u].<span class="built_in">push_back</span>(v);</span><br><span class="line">        adj[v].<span class="built_in">push_back</span>(u);</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="type">int</span> k;</span><br><span class="line">    cin&gt;&gt;k;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;k;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        cin&gt;&gt;a[i];</span><br><span class="line">        vis[a[i]]=<span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> cnt=n-k;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> u=<span class="number">0</span>;u&lt;n;++u)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(vis[u]) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> v:adj[u])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(vis[v]) <span class="keyword">continue</span>;</span><br><span class="line">            <span class="type">int</span> ru=<span class="built_in">find</span>(u),rv=<span class="built_in">find</span>(v);</span><br><span class="line">            <span class="keyword">if</span>(ru!=rv)</span><br><span class="line">            &#123;</span><br><span class="line">                fa[rv]=ru;</span><br><span class="line">                cnt--;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=k<span class="number">-1</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        ans[i]=cnt;cnt++;vis[a[i]]=<span class="number">0</span>;</span><br><span class="line">        <span class="type">int</span> u=a[i];</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> v:adj[u])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(vis[v]) <span class="keyword">continue</span>;</span><br><span class="line">            <span class="type">int</span> ru=<span class="built_in">find</span>(u),rv=<span class="built_in">find</span>(v);</span><br><span class="line">            <span class="keyword">if</span>(ru!=rv)</span><br><span class="line">            &#123;</span><br><span class="line">                fa[rv]=ru;</span><br><span class="line">                cnt--;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;cnt&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;k;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        cout&lt;&lt;ans[i]&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="典型例题：9619-米秋的贴吧吃瓜">典型例题：<a href="https://www.tzcoder.cn/acmhome/problemdetail.do?method=showdetail&amp;id=9619">9619: 米秋的贴吧吃瓜</a></h4>
<blockquote>
<p>初始有 N 个同学，M 对直接朋友关系（朋友关系具有传递性，即构成无向图）。接下来 Q 个操作按时间顺序出现：</p>
<ul>
<li>
<p><code>1 U V</code>：U 和 V 绝交（删除这条直接边）；</p>
</li>
<li>
<p><code>2 U V</code>：查询 U 和 V 当前是否还在同一个交际圈（即是否连通）。</p>
</li>
</ul>
<p>数据范围：所有测试用例的 N, M, Q 总和分别 ≤ 1.5 × 10⁵。保证初始无重边自环，每对朋友最多绝交一次。</p>
</blockquote>
<p><strong>解法思路</strong>：</p>
<p>并查集天生支持加边（合并），不支持删边。面对“边被逐步删除”的场景，常用技巧是 <strong>离线倒序处理</strong>：</p>
<ol>
<li>
<p><strong>记录所有绝交操作</strong>，把将来会被删除的初始边标记出来。</p>
</li>
<li>
<p><strong>构建最终状态</strong>：只保留那些<strong>从未被绝交过</strong>的初始边，得到最后一刻的并查集。</p>
</li>
<li>
<p><strong>从后往前回放操作</strong>：</p>
<ul>
<li>
<p>遇到查询 <code>2 U V</code>：直接用当前并查集回答，存储答案。</p>
</li>
<li>
<p>遇到绝交 <code>1 U V</code>：<strong>恢复这条边</strong>（因为倒序看，这条边是在这个时刻被删除的，往前它就是存在的），执行 <code>unite(U, V)</code>。</p>
</li>
</ul>
</li>
</ol>
<p>最后把存储的答案正序输出即可。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> maxn=<span class="number">1e5</span><span class="number">+5</span>;</span><br><span class="line"><span class="type">int</span> fa[maxn];</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(fa[x]==x) <span class="keyword">return</span> x;</span><br><span class="line">	<span class="keyword">return</span> fa[x]=<span class="built_in">find</span>(fa[x]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">	<span class="type">int</span> op,u,v;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="type">int</span> n,m,q;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">node</span>&#123;</span><br><span class="line">	<span class="type">int</span> u,v;</span><br><span class="line">&#125;;</span><br><span class="line">node adj[maxn];</span><br><span class="line">Edge a[maxn];</span><br><span class="line"><span class="type">int</span> ans[maxn];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	cin&gt;&gt;n&gt;&gt;m&gt;&gt;q;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;=n;++i) fa[i]=i;</span><br><span class="line">	map&lt;pair&lt;<span class="type">int</span>,<span class="type">int</span>&gt;,<span class="type">int</span> &gt; mp;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> u,v;</span><br><span class="line">		cin&gt;&gt;u&gt;&gt;v;</span><br><span class="line">		adj[i]=&#123;u,v&#125;;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;q;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> op,u,v;</span><br><span class="line">		cin&gt;&gt;op&gt;&gt;u&gt;&gt;v;</span><br><span class="line">		<span class="keyword">if</span>(u&gt;v) <span class="built_in">swap</span>(u,v);</span><br><span class="line">		<span class="keyword">if</span>(op==<span class="number">1</span>) mp[&#123;u,v&#125;]=<span class="number">1</span>;</span><br><span class="line">		a[i]=&#123;op,u,v&#125;;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> u=adj[i].u,v=adj[i].v;</span><br><span class="line">		<span class="keyword">if</span>(mp[&#123;u,v&#125;]) <span class="keyword">continue</span>;</span><br><span class="line">		<span class="type">int</span> ru=<span class="built_in">find</span>(u);<span class="type">int</span> rv=<span class="built_in">find</span>(v);</span><br><span class="line">		<span class="keyword">if</span>(ru!=rv) </span><br><span class="line">		&#123;</span><br><span class="line">			fa[ru]=rv;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=q<span class="number">-1</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> op=a[i].op,u=a[i].u,v=a[i].v;</span><br><span class="line">		<span class="keyword">if</span>(op==<span class="number">2</span>)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="type">int</span> ru=<span class="built_in">find</span>(u);<span class="type">int</span> rv=<span class="built_in">find</span>(v);</span><br><span class="line">			<span class="keyword">if</span>(ru!=rv) </span><br><span class="line">			&#123;</span><br><span class="line">				ans[i]=<span class="number">0</span>;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> ans[i]=<span class="number">1</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span>(op==<span class="number">1</span>)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="type">int</span> ru=<span class="built_in">find</span>(u);<span class="type">int</span> rv=<span class="built_in">find</span>(v);</span><br><span class="line">			<span class="keyword">if</span>(ru!=rv) </span><br><span class="line">			&#123;</span><br><span class="line">				fa[ru]=rv;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;q;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">int</span> op=a[i].op;</span><br><span class="line">		<span class="keyword">if</span>(op==<span class="number">2</span>)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">if</span>(ans[i]) cout&lt;&lt;<span class="string">&quot;Yes\n&quot;</span>;</span><br><span class="line">			<span class="keyword">else</span> cout&lt;&lt;<span class="string">&quot;No\n&quot;</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="典型例题：ABC447-E-Divide-Graph">典型例题：<a href="https://atcoder.jp/contests/abc447/tasks/abc447_e">ABC447 E - Divide Graph</a></h4>
<p>给定一个包含 <code>n</code> 个点、<code>m</code> 条边的带权无向图，第 <code>i</code> 条边的权值为 <code>2^i</code>（<code>i</code> 从 1 开始）。你需要删掉一些边，使得图<strong>不连通</strong>，求<strong>删掉的边权值和最小</strong>是多少。</p>
<p>数据规模：<code>1 &lt;= n &lt;= 2e5</code>，<code>1 &lt;= m &lt;= 2e5</code></p>
<p><strong>解法思路</strong>：</p>
<p>删边最小化，等价于<strong>保留边最大化</strong>——在保证图不连通的前提下，尽可能保留权值大的边。</p>
<p>由于第 <code>i</code> 条边的权值是 <code>2^i</code>，它<strong>严格大于</strong>前面所有边的权值之和（<code>2^1 + 2^2 + ... + 2^{i-1} &lt; 2^i</code>）。这个性质使得贪心策略成立：</p>
<ul>
<li>从权值<strong>从大到小</strong>（即从第 <code>m</code> 条边到第 <code>1</code> 条边）依次考虑每条边；</li>
<li>如果加入这条边<strong>不会让图变得连通</strong>（即当前连通块数量 &gt; 2），就<strong>保留</strong>它；</li>
<li>如果加入这条边会让图连通（即当前连通块数量 == 2 且这条边连接了两个不同的连通块），则<strong>不能保留</strong>，计入删掉的边权和。</li>
</ul>
<p>用<strong>并查集</strong>维护当前图的连通性，<code>cnt</code> 记录当前连通块数量。初始时 <code>cnt = n</code>。从大到小遍历所有边：</p>
<ul>
<li>若两个端点已在同一集合：这条边保留与否不影响连通性，可以直接<strong>保留</strong>（不计入答案）；</li>
<li>若两个端点在不同集合：
<ul>
<li>若 <code>cnt &gt; 2</code>：合并两个集合，<code>cnt--</code>，<strong>保留</strong>这条边；</li>
<li>若 <code>cnt == 2</code>：此时若合并，图就变成 1 个连通块（即连通了），所以这条边<strong>必须删掉</strong>，将 <code>2^i</code> 加入答案。</li>
</ul>
</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> maxn=<span class="number">2e5</span><span class="number">+5</span>,mod=<span class="number">998244353</span>;</span><br><span class="line"><span class="type">int</span> f[maxn],v[maxn],u[maxn],cost[maxn];</span><br><span class="line"><span class="type">int</span> n,m;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(f[x]==x) <span class="keyword">return</span> x;</span><br><span class="line">    <span class="keyword">return</span> f[x]=<span class="built_in">find</span>(f[x]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cin&gt;&gt;n&gt;&gt;m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) f[i]=i;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=m;i++)</span><br><span class="line">     &#123;</span><br><span class="line">	    cin&gt;&gt;u[i]&gt;&gt;v[i];</span><br><span class="line">        cost[i]=(<span class="number">1</span>&lt;&lt;i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> cnt=n,ans=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=m;i&gt;=<span class="number">1</span>;i--)</span><br><span class="line">    &#123;</span><br><span class="line">	    rv=<span class="built_in">find</span>(v[i]),ru=<span class="built_in">find</span>(u[i]);</span><br><span class="line">        <span class="keyword">if</span>(rv!=ru)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(cnt&gt;<span class="number">2</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                cnt--;</span><br><span class="line">                f[rv]=ru;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> ans=(ans+cost[i])%mod;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;ans&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>本题的关键在于两点：</p>
<ol>
<li><strong>反向思维</strong>：删边最小化 → 保留边最大化；</li>
<li><strong>特殊边权</strong>：<code>2^i</code> 的幂次性质保证了贪心策略的正确性——优先保留大边。</li>
</ol>
<p>这与前面两题的离线倒序思路有相似之处，但本题的“倒序”体现在<strong>按边权从大到小处理</strong>，而非按时间顺序倒序。</p>
<p>这道题的核心是<strong>贪心 + 并查集维护连通性</strong>，属于并查集题型中的 <strong>“动态连通性 + 贪心选择”</strong> 类别。</p>
<hr>
<h3 id="4-通过“反集”处理矛盾关系（扩展域并查集）">4. 通过“反集”处理矛盾关系（扩展域并查集）</h3>
<p>当关系不只是“同类”，还涉及“不同类”、“敌对”等二元对立关系时，可以用<strong>扩展域并查集</strong>（种类并查集）。</p>
<h4 id="典型例题：P1892-BOI2003-团伙">典型例题：<a href="https://www.luogu.com.cn/problem/P1892">P1892 [BOI2003] 团伙</a></h4>
<p>给定 n 个人和若干条“朋友”或“敌人”关系。朋友的朋友是朋友，敌人的敌人也是朋友。求最多有多少个团伙。</p>
<p><strong>解法思路</strong>：将每个人拆成两个节点——<code>i</code> 表示本人，<code>i+n</code> 表示其敌人。</p>
<ul>
<li>朋友关系：合并 <code>i</code> 和 <code>j</code></li>
<li>敌人关系：合并 <code>i</code> 与 <code>j+n</code>，合并 <code>i+n</code> 与 <code>j</code></li>
</ul>
<p>最终统计集合数量即为答案。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> maxn=<span class="number">1e3</span><span class="number">+2</span>;</span><br><span class="line"><span class="type">int</span> f[maxn*<span class="number">2</span>];</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(f[x]==x) <span class="keyword">return</span> x;</span><br><span class="line">	<span class="keyword">return</span> f[x]=<span class="built_in">find</span>(f[x]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="type">int</span> n,m;</span><br><span class="line">	cin&gt;&gt;n&gt;&gt;m;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=<span class="number">2</span>*n;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		f[i]=i;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="type">char</span> opt;</span><br><span class="line">		<span class="type">int</span> x,y;</span><br><span class="line">		cin&gt;&gt;opt&gt;&gt;x&gt;&gt;y;</span><br><span class="line">		<span class="keyword">if</span>(opt==<span class="string">&#x27;F&#x27;</span>)</span><br><span class="line">		&#123;</span><br><span class="line">			f[<span class="built_in">find</span>(x)]=<span class="built_in">find</span>(y);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			f[<span class="built_in">find</span>(y+n)]=<span class="built_in">find</span>(x);</span><br><span class="line">			f[<span class="built_in">find</span>(x+n)]=<span class="built_in">find</span>(y);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="type">int</span> cnt=<span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">if</span>(f[i]==i) cnt++;</span><br><span class="line">	&#125;</span><br><span class="line">	cout&lt;&lt;cnt&lt;&lt;endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>扩展域并查集的核心思想是<strong>拆点</strong>：一个元素拆成多个节点来表示不同的状态/角色。</p>
<h2 id="二、怎样询问这条链（集合）">二、怎样询问这条链（集合）</h2>
<h3 id="1-仅需判断连通性">1. 仅需判断连通性</h3>
<p>最基本的询问：两个元素是否在同一集合中，或连通块的数量。</p>
<h4 id="典型例题：P1536-村村通">典型例题：<a href="https://www.luogu.com.cn/problem/P1536">P1536 村村通</a></h4>
<p>给定 n 个村庄和 m 条已建道路，求至少还需要修多少条路才能使所有村庄连通。</p>
<p><strong>解法思路</strong>：用并查集统计当前连通块数量 <code>cnt</code>，需要修的道路数为 <code>cnt - 1</code>。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> maxn=<span class="number">2000005</span>;</span><br><span class="line"><span class="type">int</span> f[maxn];</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(x!=f[x]) f[x]=<span class="built_in">find</span>(f[x]);</span><br><span class="line">    <span class="keyword">return</span> f[x];</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> n,m;</span><br><span class="line">    <span class="keyword">while</span>(cin&gt;&gt;n,n)</span><br><span class="line">    &#123;</span><br><span class="line">        cin&gt;&gt;m;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i) f[i]=i;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;m;++i)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> x,y;</span><br><span class="line">            cin&gt;&gt;x&gt;&gt;y;</span><br><span class="line">            <span class="type">int</span> fx=<span class="built_in">find</span>(x);</span><br><span class="line">            <span class="type">int</span> fy=<span class="built_in">find</span>(y);</span><br><span class="line">            <span class="keyword">if</span>(fx!=fy) f[fx]=fy;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">int</span> ans=n<span class="number">-1</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(f[i]!=i) --ans;</span><br><span class="line">        &#125;</span><br><span class="line">        cout&lt;&lt;ans&lt;&lt;endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="2-需要维护集合大小-其他信息">2. 需要维护集合大小/其他信息</h3>
<p>除了连通性，有时还需要知道每个集合的大小等信息。</p>
<h4 id="典型例题：P3958-NOIP2017-提高组-奶酪">典型例题：<a href="https://www.luogu.com.cn/problem/P3958">P3958 [NOIP2017 提高组] 奶酪</a></h4>
<p>在一个三维空间中，有若干个半径相同的球形空洞。如果两个空洞相交或相切，则它们连通。判断是否存在一条从底部到顶部的通路。</p>
<p><strong>解法思路</strong>：每个空洞是一个元素。若两个空洞相交/相切则合并。另外将底部和顶部也作为特殊节点，若空洞与底部/顶部相交则合并。最终判断底部和顶部是否在同一集合中。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> maxn=<span class="number">1e3</span><span class="number">+5</span>;</span><br><span class="line">ll n,h,r;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">node</span>&#123;</span><br><span class="line">    ll x,y,z;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">dist</span><span class="params">(node a,node b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (a.x-b.x)*(a.x-b.x)+(a.y-b.y)*(a.y-b.y)+(a.z-b.z)*(a.z-b.z)&lt;=<span class="number">4ll</span>*r*r;</span><br><span class="line">&#125;</span><br><span class="line">node a[maxn];</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">DSU</span> &#123;</span><br><span class="line"><span class="comment">/* ======================================== */</span></span><br><span class="line"><span class="type">int</span> fa[maxn], siz[maxn];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">init</span><span class="params">( <span class="type">int</span> N )</span> </span>&#123; <span class="keyword">for</span>( <span class="type">int</span> i = <span class="number">0</span>; i &lt;= N; ++ i ) fa[i] = i, siz[i] = <span class="number">1</span>; &#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">( <span class="type">int</span> x )</span> </span>&#123; <span class="keyword">return</span> fa[x] == x ? x : fa[x] = <span class="built_in">find</span>( fa[x] ); &#125;</span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">merge</span><span class="params">( <span class="type">int</span> x, <span class="type">int</span> y )</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> rx = <span class="built_in">find</span>( x ), ry = <span class="built_in">find</span>( y );</span><br><span class="line">    <span class="keyword">if</span>( rx == ry ) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span>( siz[rx] &gt; siz[ry] ) <span class="built_in">swap</span>( rx, ry );</span><br><span class="line">    fa[rx] = ry, siz[ry] += siz[rx];</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">same</span><span class="params">( <span class="type">int</span> x, <span class="type">int</span> y )</span> </span>&#123; <span class="keyword">return</span> <span class="built_in">find</span>( x ) == <span class="built_in">find</span>( y ); &#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">size</span><span class="params">( <span class="type">int</span> x )</span> </span>&#123; <span class="keyword">return</span> siz[<span class="built_in">find</span>( x )]; &#125;</span><br><span class="line"><span class="comment">/* ======================================== */</span></span><br><span class="line">&#125; dsu;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cin&gt;&gt;n&gt;&gt;h&gt;&gt;r;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        ll x,y,z;</span><br><span class="line">        cin&gt;&gt;x&gt;&gt;y&gt;&gt;z;</span><br><span class="line">        a[i]=&#123;x,y,z&#125;;</span><br><span class="line">    &#125;</span><br><span class="line">    dsu.<span class="built_in">init</span>(n<span class="number">+1</span>);</span><br><span class="line">    <span class="type">int</span> flag0=<span class="number">0</span>,flagh=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(a[i].z+r&gt;=h) flagh=<span class="number">1</span>,dsu.<span class="built_in">merge</span>(i,n<span class="number">+1</span>);</span><br><span class="line">        <span class="keyword">if</span>(a[i].z-r&lt;=<span class="number">0</span>) flag0=<span class="number">1</span>,dsu.<span class="built_in">merge</span>(i,<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(!flag0 || !flagh) </span><br><span class="line">    &#123;</span><br><span class="line">        cout&lt;&lt;<span class="string">&quot;No\n&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=i<span class="number">+1</span>;j&lt;=n;++j)</span><br><span class="line">        &#123;</span><br><span class="line">            node x=a[i],y=a[j];</span><br><span class="line">            <span class="keyword">if</span>(<span class="built_in">dist</span>(x,y)) </span><br><span class="line">            &#123;</span><br><span class="line">                dsu.<span class="built_in">merge</span>(i,j);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(dsu.<span class="built_in">same</span>(<span class="number">0</span>,n<span class="number">+1</span>))  cout&lt;&lt;<span class="string">&quot;Yes\n&quot;</span>;</span><br><span class="line">    <span class="keyword">else</span> cout&lt;&lt;<span class="string">&quot;No\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="3-带权并查集：需要维护节点间的关系">3. 带权并查集：需要维护节点间的关系</h3>
<p>当题目不仅询问“是否在同一集合”，还要求知道集合内元素之间的<strong>相对关系</strong>（如距离、差值、奇偶性等）时，需要使用<strong>带权并查集</strong>。</p>
<h4 id="典型例题：P1196-NOI2002-银河英雄传说">典型例题：<a href="https://www.luogu.com.cn/problem/P1196">P1196 [NOI2002] 银河英雄传说</a></h4>
<p>有 n 列战舰，支持两种操作：将某一列整体接到另一列末尾；查询两艘战舰在同一列中的距离。</p>
<p><strong>解法思路</strong>：维护<strong>战舰 <code>x</code> 到其所在列队首（即根节点）的距离 <code>d[x]</code></strong>。合并时更新被合并列的根节点的 <code>d</code> 值。路径压缩时累加 <code>d</code> 值。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(x==fa[x]) <span class="keyword">return</span> x;</span><br><span class="line">    <span class="type">int</span> root=<span class="built_in">find</span>(fa[x]);</span><br><span class="line">    d[x]+=d[fa[x]];  <span class="comment">// 累加距离</span></span><br><span class="line">    <span class="keyword">return</span> fa[x]=root;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>完整代码</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> maxn=<span class="number">30000</span><span class="number">+5</span>;</span><br><span class="line"><span class="type">int</span> n;</span><br><span class="line"><span class="type">int</span> fa[maxn],d[maxn],sz[maxn];</span><br><span class="line"><span class="comment">//d[x]表示x 到其所在列队根节点的距离</span></span><br><span class="line"><span class="comment">//sz[x]表示x的战舰总数</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(x==fa[x]) <span class="keyword">return</span> x;</span><br><span class="line">    <span class="type">int</span> root=<span class="built_in">find</span>(fa[x]);</span><br><span class="line">    d[x]+=d[fa[x]];</span><br><span class="line">    <span class="keyword">return</span> fa[x]=root;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cin&gt;&gt;n;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=<span class="number">30000</span>;++i) fa[i]=i,d[i]=<span class="number">0</span>,sz[i]=<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(n--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">char</span> op;</span><br><span class="line">        <span class="type">int</span> x,y;</span><br><span class="line">        cin&gt;&gt;op&gt;&gt;x&gt;&gt;y;</span><br><span class="line">        <span class="type">int</span> rx=<span class="built_in">find</span>(x),ry=<span class="built_in">find</span>(y);</span><br><span class="line">        <span class="keyword">if</span>(op==<span class="string">&#x27;M&#x27;</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(rx!=ry)</span><br><span class="line">            &#123;</span><br><span class="line">                fa[rx]=ry;</span><br><span class="line">                d[rx]=sz[ry];</span><br><span class="line">                sz[ry]+=sz[rx];     </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">if</span>(rx!=ry) cout&lt;&lt;<span class="string">&quot;-1\n&quot;</span>;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                cout&lt;&lt;<span class="built_in">abs</span>(d[x]-d[y])<span class="number">-1</span>&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>查询两艘战舰的距离：若在同一集合中，答案为 <code>abs(d[a] - d[b]) - 1</code>。</p>
<h4 id="典型例题：P2024-NOI2001-食物链">典型例题：<a href="https://www.luogu.com.cn/problem/P2024">P2024 [NOI2001] 食物链</a></h4>
<p>动物王国中有三类动物 A、B、C，构成环形食物链：<strong>A 吃 B，B 吃 C，C 吃 A</strong>。现有 N 个动物（编号 1~N），用 K 句话描述它们之间的关系：</p>
<ul>
<li>
<p><code>1 X Y</code>：X 和 Y 是同类</p>
</li>
<li>
<p><code>2 X Y</code>：X 吃 Y</p>
</li>
</ul>
<p>一句话是<strong>假话</strong>当且仅当满足以下三条之一<a href="https://www.cnblogs.com/DarkValkyrie/p/10979331.html"></a><a href="https://blog.csdn.net/2201_75683257/article/details/141530324"></a>：</p>
<ol>
<li>
<p>当前的话与前面某些真的话冲突</p>
</li>
<li>
<p>当前话中 X 或 Y 比 N 大</p>
</li>
<li>
<p>当前话表示 X 吃 X（即 <code>2 X X</code>）</p>
</li>
</ol>
<p>数据范围：<code>1 ≤ N ≤ 5×10⁴</code>，<code>1 ≤ K ≤ 10⁵</code><a href="https://blog.csdn.net/2201_75683257/article/details/141530324"></a>。</p>
<p><strong>任务</strong>：输出假话的总数。</p>
<p><strong>解法思路（带权版本）</strong>：维护每个节点到根的“距离”<code>d[x]</code>，用 <code>d[x] % 3</code> 表示与根的关系（0: 同类，1: 吃根，2: 被根吃）。</p>
<p>要将 <code>x</code> 所在集合接到 <code>y</code> 所在集合下，已知 <code>x</code> 与 <code>y</code> 的关系为 <code>rel</code>（<code>rel=0</code> 同类，<code>rel=1</code> x 吃 y）：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">x-&gt;rx-&gt;ry  x-&gt;rx d[x]  rx-&gt;ry d[rx]</span><br><span class="line">x-&gt;y-&gt;ry x-&gt;y rel y-&gt;ry d[y]</span><br><span class="line">d[x] + d[rx] ≡ d[y] + <span class="built_in">rel</span> (mod <span class="number">3</span>)</span><br><span class="line">⇒ d[rx] = (d[y] + rel - d[x] + <span class="number">3</span>) % <span class="number">3</span></span><br></pre></td></tr></table></figure>
<p>完整代码</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> maxn=<span class="number">5e4</span><span class="number">+5</span>;</span><br><span class="line"><span class="type">int</span> fa[maxn],d[maxn];</span><br><span class="line"><span class="type">int</span> n,k;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(x==fa[x]) <span class="keyword">return</span> x;</span><br><span class="line">    <span class="type">int</span> p=fa[x];</span><br><span class="line">    <span class="type">int</span> root=<span class="built_in">find</span>(p);</span><br><span class="line">    d[x]=(d[x]+d[p])%<span class="number">3</span>;</span><br><span class="line">    <span class="keyword">return</span> fa[x]=root;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cin&gt;&gt;n&gt;&gt;k;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i) fa[i]=i,d[i]=<span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> ans=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(k--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> op,x,y;</span><br><span class="line">        cin&gt;&gt;op&gt;&gt;x&gt;&gt;y;</span><br><span class="line">        <span class="keyword">if</span>(x&gt;n || y&gt;n || (op==<span class="number">2</span>&amp;&amp;x==y))</span><br><span class="line">        &#123;</span><br><span class="line">            ans++;<span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">int</span> rx=<span class="built_in">find</span>(x),ry=<span class="built_in">find</span>(y);</span><br><span class="line">        <span class="type">int</span> rel=op<span class="number">-1</span>;</span><br><span class="line">        <span class="keyword">if</span>(rx==ry)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>((d[x]-d[y]<span class="number">+3</span>)%<span class="number">3</span>!=rel)</span><br><span class="line">            &#123;</span><br><span class="line">                ans++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            fa[rx]=ry;</span><br><span class="line">            d[rx]=(rel+d[y]-d[x]<span class="number">+3</span>)%<span class="number">3</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;ans&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>解法思路（扩展域版本）</strong>：将每个动物拆成三个节点——<code>x</code>（自身）、<code>x+n</code>（猎物）、<code>x+2n</code>（天敌）。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> maxn=<span class="number">50005</span>;</span><br><span class="line"><span class="type">int</span> f[<span class="number">3</span>*maxn];</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(f[x]==x) <span class="keyword">return</span> x;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> f[x]=<span class="built_in">find</span>(f[x]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">	<span class="type">int</span> n,k;</span><br><span class="line">    cin&gt;&gt;n&gt;&gt;k;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n*<span class="number">3</span>;i++) f[i]=i;</span><br><span class="line">    <span class="type">int</span> cnt=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;k;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> z,x,y;</span><br><span class="line">        cin&gt;&gt;z&gt;&gt;x&gt;&gt;y;</span><br><span class="line">        <span class="keyword">if</span>(x&gt;n||y&gt;n)</span><br><span class="line">        &#123;</span><br><span class="line">            cnt++;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(z==<span class="number">1</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(f[<span class="built_in">find</span>(x)]==f[<span class="built_in">find</span>(y+n)]||f[<span class="built_in">find</span>(y)]==f[<span class="built_in">find</span>(x+n)]) cnt++;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            &#123;</span><br><span class="line">                f[<span class="built_in">find</span>(x)]=<span class="built_in">find</span>(y);</span><br><span class="line">                f[<span class="built_in">find</span>(x+n)]=<span class="built_in">find</span>(y+n);</span><br><span class="line">                f[<span class="built_in">find</span>(x<span class="number">+2</span>*n)]=<span class="built_in">find</span>(y<span class="number">+2</span>*n);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(z==<span class="number">2</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(f[<span class="built_in">find</span>(x)]==f[<span class="built_in">find</span>(y)]||f[<span class="built_in">find</span>(y)]==<span class="built_in">find</span>(x+n)) cnt++;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            &#123;</span><br><span class="line">                f[<span class="built_in">find</span>(x)]=<span class="built_in">find</span>(y+n);</span><br><span class="line">                f[<span class="built_in">find</span>(x+n)]=<span class="built_in">find</span>(y<span class="number">+2</span>*n);</span><br><span class="line">                f[<span class="built_in">find</span>(x<span class="number">+2</span>*n)]=<span class="built_in">find</span>(y);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;cnt&lt;&lt;endl;</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>
<h4 id="要点总结">要点总结</h4>
<table>
<thead>
<tr>
<th>题型</th>
<th>维护的权值含义</th>
<th>合并公式特征</th>
<th>代表题目</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>距离/位置</strong></td>
<td>到根的距离（差值）</td>
<td><code>d[rx] = sz[ry]</code></td>
<td>P1196 银河英雄传说</td>
</tr>
<tr>
<td><strong>模数/循环</strong></td>
<td>模 k 的余数（同类/天敌）</td>
<td><code>d[rx] = (d[y] + w - d[x]) % k</code></td>
<td>P2024 食物链</td>
</tr>
<tr>
<td><strong>异或/奇偶</strong></td>
<td>0 或 1（相同/不同）</td>
<td><code>d[rx] = d[y] ^ w ^ d[x]</code></td>
<td>P5937 Parity Game</td>
</tr>
<tr>
<td><strong>比例/商</strong></td>
<td>倍数关系</td>
<td><code>d[rx] = d[y] * w / d[x]</code></td>
<td>LeetCode 399</td>
</tr>
</tbody>
</table>
<ol>
<li>
<p><strong>明确 <code>d[x]</code> 的定义</strong>：它表示 <code>x</code> 到<strong>父节点</strong>的关系，路径压缩后会变成到<strong>根</strong>的关系。</p>
</li>
<li>
<p><strong>记住 <code>find</code> 中要保存父节点</strong>：<code>int p = fa[x];</code>，这是避免错误的铁律。</p>
</li>
<li>
<p><strong>推导合并公式</strong>：画图，用向量思维 <code>x → rx → ry → y</code> 推导 <code>d[rx]</code>。</p>
</li>
<li>
<p><strong>检查模运算</strong>：C++ 负数取模为负，需要 <code>(a % mod + mod) % mod</code> 转换。</p>
</li>
<li>
<p><strong>判断何时使用</strong>：题目中出现“差值”、“距离”、“奇偶”、“倍数”、“A 吃 B”等相对关系时，优先考虑带权并查集。</p>
</li>
</ol>
<h3 id="4-扩展域并查集：需要维护多种关系-状态">4. 扩展域并查集：需要维护多种关系/状态</h3>
<p>当关系不止“同类/不同类”两种，而是有<strong>多种类型</strong>时（如食物链中的三类关系），可以用扩展域并查集。</p>
<h4 id="典型例题：P5937-CEOI1999-Parity-Game">典型例题：<a href="https://www.luogu.com.cn/problem/P5937">P5937 [CEOI1999] Parity Game</a></h4>
<p>给定一个 01 序列，每次询问区间 <code>[l, r]</code> 中 1 的个数是奇数还是偶数。判断最多前多少条询问可以同时满足。</p>
<p>数据规模：序列长度 <code>&lt;= 10^9</code>，询问数 <code>&lt;= 5000</code>。</p>
<p><strong>解法思路</strong>：用前缀和 <code>S[i]</code>。询问 <code>[l, r]</code> 中 1 的个数为奇数 ⇔ <code>S[l-1]</code> 与 <code>S[r]</code> 奇偶性不同。将每个前缀和拆成两个节点（奇/偶），用扩展域并查集维护约束。注意 <code>N</code> 很大但询问数很少，需要<strong>离散化</strong>。</p>
<p><strong>带权 vs 扩展域的选择</strong>：</p>
<ul>
<li><strong>带权并查集</strong>：适合维护<strong>数值关系</strong>（距离、差值等），如银河英雄传说；</li>
<li><strong>扩展域并查集</strong>：思维量较小，适合维护<strong>逻辑关系</strong>（同类/不同类/多种分类），如食物链、关押罪犯、Parity Game。</li>
</ul>
<h3 id="5-并查集与其他算法结合">5. 并查集与其他算法结合</h3>
<p>并查集常作为工具与其他算法配合使用：</p>
<ul>
<li><strong>Kruskal 最小生成树</strong>：用并查集判断加入边是否成环；</li>
<li><strong>离线查询 + 排序</strong>：将询问按某种顺序排序后动态维护连通性；</li>
<li><strong>ST 表优化建图</strong>：对于区间一一对应的约束，用 ST 表分块 + 并查集优化连边。</li>
</ul>
<h2 id="三、题型框架总结">三、题型框架总结</h2>
<table>
<thead>
<tr>
<th>题型分类</th>
<th>核心特征</th>
<th>代表题目</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>模板/基础连通性</strong></td>
<td>合并与查询</td>
<td>P3367 【模板】并查集</td>
</tr>
<tr>
<td><strong>普通连通性</strong></td>
<td>求连通块数量/判断连通</td>
<td>P1551 亲戚、P1536 村村通、P3958 奶酪</td>
</tr>
<tr>
<td><strong>离线倒序</strong></td>
<td>删边转加边</td>
<td>P1197 星球大战、米秋的贴吧吃瓜、ABC447E</td>
</tr>
<tr>
<td><strong>维护集合信息</strong></td>
<td>集合大小等附加信息</td>
<td>P3958 奶酪</td>
</tr>
<tr>
<td><strong>扩展域并查集</strong></td>
<td>拆点表示多种状态/关系</td>
<td>P1892 团伙、P1525 关押罪犯、P2024 食物链、P5937 Parity Game</td>
</tr>
<tr>
<td><strong>带权并查集</strong></td>
<td>维护节点到根的数值关系</td>
<td>P1196 银河英雄传说、P2024 食物链</td>
</tr>
<tr>
<td><strong>并查集 + 其他算法</strong></td>
<td>作为子模块配合使用</td>
<td>Kruskal 最小生成树、ST 表优化建图</td>
</tr>
</tbody>
</table>
<p>并查集本身代码极短，难点在于<strong>抽象能力</strong>——能否把原始问题抽象成集合的合并与查询。做题时可以先问自己三个问题：</p>
<ol>
<li><strong>谁是元素？</strong>——问题中的基本对象是什么；</li>
<li><strong>什么算连通？</strong>——两个对象在什么条件下属于同一集合；</li>
<li><strong>问什么？</strong>——是问连通性、集合大小、相对关系，还是多种状态的分类？</li>
</ol>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>洛谷P4551 最长异或路径</title>
    <url>/2026/08/09/%E6%B4%9B%E8%B0%B7P4551/</url>
    <content><![CDATA[<p><a href="https://www.luogu.com.cn/problem/P4551">P4551 最长异或路径 - 洛谷</a></p>
<h2 id="题意">题意</h2>
<p>给你一棵 <strong>带权树</strong>（每个边有一个权值），让你找到<strong>树上任意两个节点之间的路径</strong>，使得这条路径上<strong>所有边权的异或和（XOR）最大</strong>，并输出这个最大值。</p>
<h2 id="思路">思路</h2>
<p><strong>树上任意两点 <code>u</code> 和 <code>v</code> 之间路径的异或和，等于 <code>u</code> 到根节点的异或和 异或上 <code>v</code> 到根节点的异或和</strong></p>
<p><strong>Why？！</strong> 
因为从根节点到 u，和从根节点到 v 的路径中，<strong>重复的部分（即从根节点到它们最近公共祖先 LCA 的那一段）会被异或两次</strong>。而根据异或的性质 <code>a ^ a = 0</code>，这一段就抵消掉了，剩下的正好是 u 到 v 的唯一路径。</p>
<ul>
<li>
<p><strong>操作</strong>：任选一个根（比如节点 1），跑一遍 DFS，计算出每个节点到根的异或值，存入数组 <code>dis</code> 中。</p>
</li>
<li>
<p><strong>结果</strong>：问题成功从“树上找两条路径”<strong>降维</strong>成了“在数组 <code>dis</code> 中找两个数，使它们的异或值最大”。</p>
</li>
</ul>
<p><strong>转化后的经典问题（最大异或对）</strong><a href="https://www.luogu.com.cn/problem/P10471">P10471 最大异或对 The XOR Largest Pair - 洛谷</a></p>
<p>现在你手里有一个数组 <code>dis</code>，里面全是整数。你需要找出 <code>dis[i] ^ dis[j]</code> 的最大值。
如果暴力枚举所有数对（<code>O(n^2)</code>），对于 <code>n=100000</code> 的数据必然超时。</p>
<p><strong>接下来我们就需要利用 01-Trie 进行贪心查找</strong></p>
<p>01-Trie 本质上是一棵二叉树，每个节点只有 <code>0</code> 和 <code>1</code> 两个分叉，用来存储数字的二进制位。</p>
<ol>
<li>
<p><strong>插入（建树）</strong>：将数组 <code>d</code> 中的每个数字，从**高位（第 30 位）到低位（第 0 位）**依次插入 Trie 树中。</p>
</li>
<li>
<p><strong>查询（贪心）</strong>：遍历数组 <code>d</code> 中的每个数字 <code>x</code>，在 Trie 树中查找与它异或结果最大的那个数。</p>
<ul>
<li>
<p><strong>贪心策略</strong>：为了让异或结果最大，<strong>高位优先</strong>要变成 <code>1</code>。所以对于 <code>x</code> 的当前位 <code>bit</code>，我们优先走 <code>bit ^ 1</code> 的分支（相反方向）。</p>
</li>
<li>
<p>如果有相反分支，说明这一位异或结果是 <code>1</code>，累加到答案中，走过去。</p>
</li>
<li>
<p>如果没有相反分支，只能走相同的分支，这一位异或结果是 <code>0</code>，走过去。</p>
</li>
</ul>
</li>
<li>
<p>记录每次查询结果的最大值，就是最终答案。</p>
</li>
</ol>
<h2 id="代码">代码</h2>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> maxn=<span class="number">1e5</span><span class="number">+5</span>,maxm=<span class="number">1e5</span>*<span class="number">32</span><span class="number">+5</span>;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Trie01</span>&#123;</span><br><span class="line"><span class="type">int</span> ch[maxm][<span class="number">2</span>],tot;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">init</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;=tot;++i) ch[i][<span class="number">0</span>]=ch[i][<span class="number">1</span>]=<span class="number">0</span>;</span><br><span class="line">    tot=<span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">insert</span><span class="params">(<span class="type">int</span> x)</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> u=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">30</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> v=(x&gt;&gt;i)&amp;<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span>(!ch[u][v]) ch[u][v]=++tot;</span><br><span class="line">        u=ch[u][v];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">query</span><span class="params">(<span class="type">int</span> x)</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> u=<span class="number">0</span>,res=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">30</span>;i&gt;=<span class="number">0</span>;--i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> v=(x&gt;&gt;i)&amp;<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span>(ch[u][v^<span class="number">1</span>])</span><br><span class="line">        &#123;</span><br><span class="line">            res|=(<span class="number">1</span>&lt;&lt;i);</span><br><span class="line">            u=ch[u][v^<span class="number">1</span>];</span><br><span class="line">        &#125;<span class="keyword">else</span> u=ch[u][v];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br><span class="line">&#125;trie01;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">    <span class="type">int</span> v;ll w;</span><br><span class="line">&#125;;</span><br><span class="line">vector&lt;Edge&gt; adj[maxn];</span><br><span class="line"><span class="type">int</span> dis[maxn];<span class="comment">//每个点到根的距离的异或和</span></span><br><span class="line"><span class="type">int</span> n;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> u,<span class="type">int</span> p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> [v,w]:adj[u])</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(v==p) <span class="keyword">continue</span>;</span><br><span class="line">        dis[v]=dis[u]^w;</span><br><span class="line">        <span class="built_in">dfs</span>(v,u);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cin&gt;&gt;n;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;n<span class="number">-1</span>;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> u,v,w;</span><br><span class="line">        cin&gt;&gt;u&gt;&gt;v&gt;&gt;w;</span><br><span class="line">        adj[u].<span class="built_in">push_back</span>(&#123;v,w&#125;);</span><br><span class="line">        adj[v].<span class="built_in">push_back</span>(&#123;u,w&#125;);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">dfs</span>(<span class="number">1</span>,<span class="number">0</span>);</span><br><span class="line">    trie<span class="number">01.</span><span class="built_in">init</span>();</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i) trie<span class="number">01.</span><span class="built_in">insert</span>(dis[i]);</span><br><span class="line">    <span class="type">int</span> ans=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">    &#123;</span><br><span class="line">        ans=<span class="built_in">max</span>(ans,trie<span class="number">01.</span><span class="built_in">query</span>(dis[i]));</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;ans&lt;&lt;<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ios::<span class="built_in">sync_with_stdio</span>(<span class="literal">false</span>);</span><br><span class="line">    cin.<span class="built_in">tie</span>(<span class="literal">nullptr</span>);</span><br><span class="line">    <span class="type">int</span> ___T=<span class="number">1</span>;</span><br><span class="line">    <span class="comment">//cin&gt;&gt;___T;</span></span><br><span class="line">    <span class="keyword">while</span>(___T--)  <span class="built_in">solve</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>算法竞赛</category>
      </categories>
      <tags>
        <tag>算法</tag>
        <tag>图</tag>
      </tags>
  </entry>
  <entry>
    <title>关于</title>
    <url>/about/index.html</url>
    <content><![CDATA[<h1 id="你好，我是梵诗-👋">你好，我是梵诗 👋</h1>
<p>一名算法竞赛爱好者，主攻 <strong>C++</strong>。平时喜欢刷题、补题、整理题解。</p>
<ul>
<li>📌 常用 OJ：AtCoder、洛谷</li>
<li>📝 博客内容：AtCoder 题解、图论 / 数据结构学习笔记</li>
<li>🐙 GitHub：<a href="https://github.com/lhhyyy666">lhhyyy666</a></li>
</ul>
<div class="note blue icon-padding flat"><i class="note-icon fas fa-fan"></i><p>海压竹枝低复举，风吹山角晦还明。—— 记录我的刷题之路</p>
</div>
<div class="note green icon-padding flat"><i class="note-icon fas fa-bullhorn"></i><p>博客所有题解均为个人理解，如有错误欢迎在评论区指出，一起交流进步！</p>
</div>
]]></content>
  </entry>
  <entry>
    <title>友链</title>
    <url>/link/index.html</url>
    <content><![CDATA[<!-- 添加友链：在下面 {% flink %} 的 link_list 里按格式添加，欢迎互换友链～ -->
<div class="flink"><div class="flink-name">我的小伙伴</div><div class="flink-desc">一起刷题，一起进步</div><div class="flink-list">
      <div class="flink-list-item">
        <a href="https://lhhyyy666.github.io" title="梵诗" target="_blank">
          <div class="flink-item-icon">
            <img class="no-lightbox" src="/img/me.jpg" onerror='this.onerror=null;this.src="/img/banner-mist.jpg"' alt="梵诗" />
          </div>
          <div class="flink-item-name">梵诗</div>
          <div class="flink-item-desc" title="梵诗的奇思妙想">梵诗的奇思妙想</div>
        </a>
      </div>
      <div class="flink-list-item">
        <a href="https://raincnya.space/" title="米秋" target="_blank">
          <div class="flink-item-icon">
            <img class="no-lightbox" src="https://raincnya.space/img/avatar.jpg" onerror='this.onerror=null;this.src="/img/banner-mist.jpg"' alt="米秋" />
          </div>
          <div class="flink-item-name">米秋</div>
          <div class="flink-item-desc" title="一个有趣的朋友">一个有趣的朋友</div>
        </a>
      </div>
      <div class="flink-list-item">
        <a href="https://aliluil.github.io/" title="iluil" target="_blank">
          <div class="flink-item-icon">
            <img class="no-lightbox" src="https://aliluil.github.io/images/avatar.jpg" onerror='this.onerror=null;this.src="/img/banner-mist.jpg"' alt="iluil" />
          </div>
          <div class="flink-item-name">iluil</div>
          <div class="flink-item-desc" title="一个可爱的朋友">一个可爱的朋友</div>
        </a>
      </div></div></div>
]]></content>
  </entry>
  <entry>
    <title>分类</title>
    <url>/categories/index.html</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>标签</title>
    <url>/tags/index.html</url>
    <content><![CDATA[]]></content>
  </entry>
</search>
